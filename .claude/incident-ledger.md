# Ledger de Incidentes

**Append-only.** Registro FACTUAL de incidentes/near-misses (proceso, bugs, verificación fallida…).
Vive en `.claude/` → **NO se auto-carga** (sin coste de tokens por sesión). **No editar entradas pasadas; solo añadir** (más reciente arriba).

**Uso:** materia prima de la auto-mejora. **Solo `/optimize`** promueve una entrada a regla, y solo con **≥2 incidencias independientes** de la misma clase, **o 1 de clase irreversible** (dinero/datos/seguridad/PII).

**Formato:** `- [id] fecha · clase · qué pasó · evidencia · estado`
**Estado:** `pendiente (>=2)` = registrado, sin promover · `promovido -> <regla>` = ya es regla.

<!-- entradas nuevas debajo, más reciente arriba -->

> **Promoción del 2026-08-10 (`/optimize`).** Cuatro clases superaron el umbral y ya son regla:
> · **instrumento no calibrado** (3 incidencias: SYNC-01, INSTR-01, INSTR-02) → `produccion-loop.md` §D, regla madre
> · **régimen de validación equivocado** (3: ANCLA-01, BASURA-01, WPM-01) → `produccion-loop.md` §C, "el techo del gate"
> · **fix no propagado al gemelo** (2: PATH-02, LOG-02) → `decision-making.md` §11, tabla de pares + grep obligatorio
> · **garantía existente pero no aplicada** (2: GUARD-01, BASURA-01; 4.º episodio de §17) → `decision-making.md` §17, 2.º corolario
> Sin promover (n=1, reversibles, ya arregladas): DOC-01, COMA-01, GATE-01. DOC-01 sí motivó **corregir el
> dato caducado en los 6 ficheros donde seguía vivo** — la corrección de un hecho no es una regla nueva.

- [DOC-01] 2026-08-10 · doc-caducada · `CLAUDE.md` §OpenRouter afirmaba "0 créditos comprados, saldo −0,06 USD, 50 peticiones/día" cuando la realidad eran 10 créditos y 1000/día. **Tres revisiones adversariales independientes repitieron el dato falso** como hecho verificado porque estaba escrito en `CLAUDE.md`; el SEED, que decía lo correcto, fue acusado por las tres de tener una premisa falsa · evidencia: `GET https://openrouter.ai/api/v1/credits` → `{'total_credits': 10, 'total_usage': 0.063839}`, `is_free_tier: False`; `CLAUDE.md:380` (antes del fix) · estado: pendiente (>=2)

- [SYNC-01] 2026-08-10 · medición-invalida · Al comparar la versión vieja y la nueva de `_ensure_breathing_commas` usé la métrica `exceso = nº silencios − nº signos de puntuación`. El número de comas insertadas está en el denominador, así que la métrica penaliza automáticamente a la variante que inserta menos comas: dio "5 vs 10 pausas inventadas" cuando los silencios crudos eran **30 vs 31** (indistinguibles). Casi cierro un cambio como regresión por un artefacto de mi propia métrica · evidencia: `ffmpeg silencedetect n=-35dB:d=0.25` sobre las dos síntesis, 420 palabras del mismo texto · estado: **promovido → `produccion-loop.md` §D (regla madre: calibra el instrumento contra un caso conocido)**

- [PATH-02] 2026-08-10 · bug-gemelo-no-revisado · `gameplay_pool._concat_videos:74` escribía rutas RELATIVAS en el fichero de lista del demuxer `concat`. Es el **gemelo exacto** del bug arreglado en `video_cleaner._concat_segments`, y la regla 11 del repo ("bug en un path → revisa los paths análogos") existía ya cuando se aplicó aquel fix. Se dispara desde `take_chunk` con 2+ ficheros en el pool = el caso normal de producción; el fixture E2E tiene 1 solo fichero y nunca lo ejerció · evidencia: repro con 2 clips en `./pool` + lista en `./temp` → `FALLO: Error concatenating`; tras `os.path.abspath` → `OK duration=2.000000`; integración con 3 trozos de gameplay real → chunk de 152,6 s, desvío 0,00 s · estado: **promovido → `decision-making.md` §11 (tabla de pares gemelos + grep obligatorio)**

- [LOG-02] 2026-08-10 · error-ilegible · 4 sitios de `gameplay_pool.py` registraban `stderr[:300]` de FFmpeg, que son el **banner de compilación**: el error real quedaba fuera del log. Es el mismo defecto ya documentado y arreglado en `video_cleaner`, no propagado al módulo hermano · evidencia: el repro de [PATH-02] devolvió `ffmpeg version 8.1-full_build... configuration: --enable-gpl...` en vez de la causa · estado: **promovido → `decision-making.md` §11 (tabla de pares gemelos)**

- [GUARD-01] 2026-08-10 · garantia-en-prosa · Dos garantías estaban escritas y no impuestas: (a) `_validar_salida` no se llamaba en `_generate_continuation`, así que en una historia de 30 min (4-6 bloques) el guardia anti-razonamiento cubría solo el primero; (b) el invariante "`_ensure_breathing_commas` no cambia el número de palabras" vivía en un comentario, sin `assert`, pese a que `main.py` cuenta palabras para arrancar la intro · evidencia: `grep -rn "_validar_salida" modules/` → solo `script_generator.py:253` y `shorts_generator.py:93`; property test nuevo: 0 violaciones en 1.248 comprobaciones · estado: **promovido → `decision-making.md` §17 (2.º corolario: un guardia que existe no está aplicado)**

- [COMA-01] 2026-08-10 · heuristico-incorrecto · `_CONECTORES_LARGOS` insertaba comas gramaticalmente incorrectas en español: partía locuciones ("antes de, que"), separaba verbo y complemento ("amenazando, con destruir") y convertía relativas especificativas en explicativas ("el primer sueldo, que mi padre ganó"), que **cambia el significado**. Además `_CONECTORES_SEGUROS` tenía "aunque" 5 veces de 12 entradas · evidencia: auditoría sobre 7.399 palabras de texto español generado; 68 de 92 inserciones (74%) salían del grupo ambiguo · estado: pendiente (>=2)

- [WPM-01] 2026-08-10 · calibrado-en-regimen-equivocado · `target_wpm: 195` se calibró con clips de 3 min, donde el modelo se pasa y manda `_truncate_to_words`; a 30 min manda su propia capacidad y el ratio real proyectado es 1,21 (≈6 min de gameplay repetido). Además `main.py:80` usaba `story.target_wpm` para dimensionar los SHORTS, así que el cambio 150→195 subió el gasto de 33 a 43 peticiones por vídeo (+30%) **sin reportarse**, contra la regla explícita del repo · evidencia: `pipeline.log` n=10, wpm real 200 en textos cortos vs 160,6 en la única producción larga (5047 palabras / 1885 s) · estado: **promovido → `produccion-loop.md` §C (régimen de validación)**; el valor del knob sigue abierto (n=2: 160,6 y 177,2)

- [GATE-01] 2026-08-10 · gate-destruye-su-entrada · `/eval` no podía medirse a sí mismo: `cleanup_temp` (`utils.py:76`, llamado en `main.py:397`) borra `temp/` al terminar una corrida con éxito, y ahí viven el `.ass` y el `_story.txt` que son **exactamente** lo que el gate mide. La primera corrida del gate solo pudo medirse porque copié los artefactos a mano mientras el pipeline corría. Consecuencia mayor: **ninguna corrida de producción era auditable a posteriori** · evidencia: `ls test_e2e/temp/` vacío tras la corrida; artefactos rescatados en paralelo · arreglado con `--keep-temp` · estado: pendiente (>=2)

- [INSTR-01] 2026-08-10 · instrumento-roto · Mi primera versión de `pausas_fuera_de_puntuacion` en `scripts/eval_sync.py` calculaba el hueco como `siguiente.start - previa.start`, que es la **duración de la palabra**, no el silencio. Reportó **73 pausas fuera de puntuación**; con `siguiente.start - previa.end` son **1**. Segundo error de medición mío en la misma sesión (ver [SYNC-01]): la clase es "métrica plausible no validada contra un caso conocido" · evidencia: `DESPACHO` cue 9.35→9.77 daba "hueco 0,42 s" sin haber silencio alguno · estado: **promovido → `produccion-loop.md` §D (regla madre)**

- [BASURA-01] 2026-08-10 · vídeo-publicable-roto · En la primera corrida de producción real (29,85 min) el modelo escupió a mitad de la historia `"de onean need that the writing plan2:en300 five the 02 0230 the0222 0207- 01=، the 02201 the"`, y eso se **narró y se subtituló**: sale en pantalla del minuto 15:38 al 15:47 de un vídeo publicable. Los guardias existentes (`_validar_salida`, y el `_validar_continuacion` añadido ESE MISMO DÍA) solo inspeccionan la CABECERA de la salida, porque el modo de fallo conocido era el razonamiento al principio; este está enterrado en el cuerpo · evidencia: `temp/video_001_story.txt` palabras 2778-2798; `temp/video_001_subs.ass:2756,2765,2767` (`ONEAN`, `0230`, `0207-`) · arreglado con `_detectar_basura` (ventana deslizante de 60 palabras, ≥3 tokens anómalos DISTINTOS), 0 falsos positivos sobre ~12.700 palabras de español real · estado: **promovido → `produccion-loop.md` §C (régimen) + `decision-making.md` §17 (2.º corolario: el guardia miraba solo la cabecera)**

- [ANCLA-01] 2026-08-10 · desfase-real-nunca-medido · Primera medición de sincronismo a escala de producción (214 frases frente a las ≤19 del fixture): **4 ventanas discretas** con frases enteras ~1 s por detrás de la voz. Causa raíz localizada por `bug-hunter` y validada por el orquestador: `tts_engine.py:458` calcula la traslación de toda la ventana con UNA sola palabra (`offset = s_start - w_first`); si el alineador mete esa primera palabra dentro del silencio previo, el error se propaga rígido a las ~94 palabras siguientes · evidencia: fragmentos frescos de 40 s → 1000-1040 s mediana **+1,050 s** con 90 de 125 palabras >0,5 s, control 300-340 s **−0,110 s**; descartados por medición edge-tts (214/214 anclas a ±0,5 ms), composición (−0,100 s exactos) y `subtitle_builder` (±9 ms) · **fix propuesto, NO aplicado**: el fixture de 3 min no puede verlo · estado: **promovido → `produccion-loop.md` §C (el techo del gate)**; ⚠️ el BUG SIGUE VIVO en `tts_engine.py:458` — bloque B de `SEED_sincronismo_produccion.md`

- [INSTR-02] 2026-08-10 · instrumento-roto · `scripts/eval_sync.py` empareja con `difflib` GLOBAL sobre 5000+ palabras y engancha ocurrencias lejanas donde el texto se repite, fabricando retraso inexistente. Reportó desfase en 1245-1285 s, 1585-1625 s y casi todo 605-645 s; transcripciones frescas de 40 s de esas zonas dan **−0,080 s** (sanas). Infló la media global de 0,072 a 0,153 s y volteó el sesgo. Tercer error de medición propio en la misma sesión · evidencia: tabla de 5 zonas, `validar_veredicto.py` · **pendiente**: emparejado local por ventanas (bloque A de `SEED_sincronismo_produccion.md`) · estado: **promovido → `produccion-loop.md` §D (regla madre)**; el arreglo del script sigue abierto

- [DEDUP-01] 2026-08-10 · vídeo-publicable-roto · La primera producción real **narra dos veces el mismo párrafo**: 83 palabras repetidas literalmente en `temp/video_001_story.txt` (posiciones 2687 y 2792) y 72 n-gramas de 12 duplicados. `script_generator.py:492` concatenaba `story + "\n\n" + continuation` sin comparar el bloque nuevo con lo ya escrito, y `_validar_continuacion` juzga el bloque **aislado**, así que ningún guardia podía verlo. Efecto colateral medido por el agente ciego: al quedar el bloque 2 cortado sin puntuación terminal, el separador de frases fusionó el tramo en una frase de 391 palabras. Ni el SEED v1 ni las tres revisiones previas lo nombraban · evidencia: `python - <<'PY'` sobre `temp/video_001_story.txt` → `n-gramas de 12 repetidos: 72`, `primer tramo repetido: pos 2687 y pos 2792, longitud 83 palabras` · arreglado con `_strip_duplicated_opening` (≥12 palabras consecutivas idénticas, solo al arranque del bloque); validado: corta 84 palabras en la costura real, **0 falsos positivos en 200 costuras arbitrarias** del mismo texto español y en los 50 shorts · estado: pendiente (>=2)

- [ANCLA-02] 2026-08-10 · diagnostico-incompleto · El fix que proponía `SEED_sincronismo_produccion.md` v1 para ANCLA-01 ("si el paso de la 1.ª a la 2.ª palabra supera un umbral, adelanta el resto de la ventana ese exceso") **no podía funcionar**: el paso no predice el error, y hay inversión de rango — paso 2,11 s → error 1,07 s, pero paso 1,98 s → error **1,48 s**. Con K=0,55 quedan residuos de −0,49 a +0,33 y 6 falsos positivos (vocativos con coma = pausa legítima); con K=1,20, residuos de +0,16 a +0,98. La razón es que hay **DOS modos de fallo** y el paso los conflaba: (1) ancla corrupta —Whisper mete la 1.ª palabra en el silencio previo— y (2) **silencio inventado DENTRO de la ventana**, donde el ancla es correcta y lo roto es el ritmo interior (`'El silencio que siguió fue absoluto.'`, 6 palabras y 2,91 s según edge-tts, salió con 2,09 s entre la 1.ª y la 2.ª palabra cuando la voz las dice a 0,36 s). Además el fix propuesto generaba **solapes** de subtítulo que nada cortaba · evidencia: `scripts/anchor_bench.py bench`; barrido del umbral interno sobre 214 ventanas contra transcripción independiente · arreglado con offset = mediana de residuos del vecindario (`ANCLA_TOL=0.40`) + cierre de huecos internos (`ANCLA_HUECO_MAX=0.80`) + `_enforce_monotonic`: palabras desincronizadas **204 → 20**, vídeo afectado **60,3 s → 7,2 s**, p95 **1,010 → 0,286 s**, sesgo **+0,005 → −0,040 s**, solapes **2 → 0** · estado: pendiente (>=2)

- [INSTR-04] 2026-08-10 · acusacion-no-reproducible · **[INSTR-02] atribuyó a `scripts/eval_sync.py` un fallo que ese código no tiene.** Decía que su `difflib` global "infló la media de 0,072 a 0,153 s y volteó el sesgo", y el SEED ordenaba reescribir el JSON de la corrida larga por estar "mal". Medido A/B sobre los mismos artefactos, el emparejador global y uno local por ventanas dan **resultados idénticos** (5062 pares, |err| medio 0,153 s, sesgo +0,003 s) y **los 6 veredictos de zona coinciden** con las transcripciones frescas de 40 s, incluidas las tres que INSTR-02 daba por falsas. El 0,072 era el baseline del **fixture de 3 min**: se comparó con producción sin decir que son dos regímenes distintos, y la subida real la causa ANCLA-01 (204 palabras a ~+1 s), no el instrumento. El script citado como evidencia (`validar_veredicto.py`) ya no existe en el repo · evidencia: A/B pegado en la sesión; `data/eval/2026-08-10-produccion-real.json` **conservado intacto** y la re-medición escrita aparte en `-v2.json` · consecuencia útil: el emparejado local se adoptó igual (no puede enganchar repeticiones lejanas, coste 0) y se añadió al gate la métrica que sí ve esta clase de fallo — `peor_tramo` (mediana del tramo de 40 palabras más desincronizado): **+1,110 s en t=690 s** donde la media decía 0,153 s y el sesgo +0,003 s · estado: pendiente (>=2)

- [ANCLA-03] 2026-08-11 · fix-que-rompe-otra-cosa · La primera versión del anclaje robusto (mediana del vecindario) descartaba el ancla SIEMPRE que el residuo se apartaba de sus vecinos. Eso rompió **3 de 16 shorts** de producción: `short_008` con un tramo entero a **−1,98 s** de la voz y `short_004` con sesgo **+0,132 s** y 15 de 122 palabras >0,5 s tarde, mientras el vídeo largo mejoraba. La causa: un residuo grande tiene DOS orígenes opuestos —ancla corrupta (outlier AISLADO, vecinos de acuerdo) y **deriva del alineador** (residuos que crecen en ventanas SEGUIDAS: −3,47 y luego −5,39)— y en el segundo el ancla de edge-tts es la CURA, no el problema · evidencia no circular: `silencedetect` sobre el audio del short da silencios que acaban en **39,596 s y 45,918 s**, justo donde edge-tts arranca esas frases (39,41 / 45,75), mientras Whisper las ponía en 42,88 y 51,14 · arreglado exigiendo que el outlier sea aislado; medido después: ayer 204 palabras → 15, hoy peor ventana 7,43 → 1,45 s · **lección**: el vídeo largo daba verde a un fix que rompía los shorts; sin medir el gemelo se publica igual · estado: pendiente (>=2)

- [ANCLA-04] 2026-08-11 · alineador-aplasta-ventana · En la corrida del 11-ago, ~40 palabras salieron hasta **7,4 s POR DELANTE** de la voz. Whisper metió 44 palabras en **4,98 s** cuando edge-tts dice que esa frase dura **13,49 s** (530 wpm, imposible de pronunciar): el ancla las devolvía al inicio correcto pero seguían apelotonadas, así que el subtítulo se vaciaba en 5 s mientras la voz tardaba 13. El código contemplaba el caso contrario (Whisper más largo que la ventana → comprimir) pero no este · evidencia: pares ASS/voz con separación de exactamente 0,05 s (el `paso_min` de la guarda de monotonía desplegando palabras colapsadas); distribución del wpm implícito sobre 324 ventanas de dos producciones: p50 209-225, p99 268-300, patológicas 530 y 833 · arreglado repartiendo la ventana cuando supera 330 wpm · **descartado por medición**: reescalar la ventana cuando ocupa más de lo que se tarda en decirla empeora con todos los umbrales (ayer 3 → 6 → 14 → 23 ventanas malas con 1.5/1.25/1.15) · estado: pendiente (>=2)

- [COMA-02] 2026-08-11 · una-causa-tres-sintomas · El modelo entregó una historia de 5334 palabras con **10 comas** (0,2 por 100) y frases de mediana **42 palabras**; la del día anterior traía 448 comas (8,5 por 100) y frases de 13. `_ensure_breathing_commas` solo subió de 10 a 146 (2,7 por 100), **un tercio** de la densidad natural, porque solo inserta delante de conectores y con frases de 42 palabras no hay suficientes. De ahí salen encadenados los tres síntomas que parecían independientes: **101 pausas fuera de puntuación** (3,9/min frente a 1,1 el día anterior, mediana 0,44 s = la pausa que edge-tts se inventa), la voz a **202 wpm** (frente a 177 y 160,6) y el ratio vídeo/chunk **0,79** (7 min de gameplay sin usar) · consecuencia: `target_wpm` NO es calibrable, porque la velocidad la fija la densidad de comas y no una constante — con n=3 el rango medido es 160,6 / 177,2 / 202,3 · evidencia: `_clean_speech_for_tts` sobre las dos historias reales, conservadas en `data/evidence/` · estado: pendiente (>=2)

- [CIERRE-01] 2026-08-11 · vídeo-publicable-roto · **La historia de la corrida del 11-ago no tiene final**: 26,4 min de historia de venganza que acaban con el protagonista subiendo una escalera (*"…y que yo voy a entrar y nada ni nadie me va a detener."*), sin sentencia, sin confrontación y sin consecuencia — el pago es exactamente lo que el espectador viene a ver. **Dos caminos independientes la decapitaban**, los dos medidos: (a) el `while` de `generate_story` (`script_generator.py:537`) evalúa su condición en la cabecera, así que un bloque de "continuación" que cruce el 85% del objetivo termina el bucle **sin que se haya pedido nunca `is_final`** — el bloque 1 dio 1768 palabras, la continuación devolvió 3566 de golpe y el bucle salió; (b) `_truncate_to_words` se quedaba con las **primeras** `max_words` palabras, así que la corrida del 10-ago (que sí pidió su bloque final) descartó **1867 palabras (26%)** con el epílogo dentro, y que aquello acabara en algo que parece un cierre fue suerte de dónde cayó el corte · frecuencia: **1 de las 3 corridas largas** del log falla por (a) — depende solo de cuánto escriba el modelo en el bloque 1, es una lotería por corrida · evidencia: `pipeline.log:5330-5336` (`Bloque 2/3: … pidiendo continuación` → `Bloque 2: total acumulado 5334` → `Historia completa`, sin bloque final); `data/evidence/video_001_story_11ago.txt` últimas 60 palabras; log del 10-ago `Bloque 3: total acumulado 7157` → `Truncando de 7157 a ~5345` · ningún guardia podía verlo: `_validar_salida` juzga cada bloque **aislado** y `scripts/audit_run.py` no mide cierre narrativo · arreglado: un `if` fuerza el bloque de desenlace si el bucle salió sin él y **aborta el vídeo** (`RuntimeError`) si ese bloque vuelve por debajo de 120 palabras, en vez de publicarlo sin final; `_truncate_to_words` recorta el **cuerpo** y conserva las últimas ~600 palabras (tope: un tercio del objetivo, para que el fixture de `/eval` no se quede solo con el epílogo) · efecto colateral arreglado de paso: el troceo de frases rejuntaba con `" ".join()`, lo que partía `"4.500"` en `"4. 500"` y **borraba todos los saltos de párrafo** — justo lo que edge-tts usa para no cortar a mitad de frase en sus trozos de 4096 bytes · verificado por ejecución: 22/22 comprobaciones incluidos los degenerados (texto vacío, sin puntuación, `max_words`=0/1, objetivo menor que el cierre) y la no-regresión del camino normal (1 bloque final, 0 peticiones extra); el caso del 11-ago pasa de `[(False, 3566)]` a `[(False, 3566), (True, 300)]` · coste: **+1 petición por vídeo solo cuando el bucle sale sin cierre**; en el fixture de `/eval` dispara siempre (el bloque 1 ya cruza el 85% de sus 480 palabras), así que el gate pasa de ~5 a ~6 peticiones · estado: pendiente (>=2)

- [REVIEW-01] 2026-08-11 · referencia-equivocada · Un panel adversarial de 4 agentes (`/seed-review`) sobre `SEED_comas_y_cierre.md` produjo **dos falsos positivos de bulto, ambos por comparar contra la referencia equivocada**, y los dos se colaron hasta el veredicto que se le enseñó al usuario: (1) *"el techo de bitrate no se propagó a los shorts, 12/14 fuera de norma"* — falso: `shorts_generator.py:294` tiene su propio `-maxrate 12M`, deliberado y comentado, porque los shorts son 1080x1920@60 y ésa es la recomendación de YouTube; el `8M` es del vídeo largo, que es **720p**. Medido: 11,59 y 12,01 Mbps = **en su techo, funcionando** (antes iban a 23,6). Se comparó contra el umbral **de otro formato**; (2) *"19,4 s de subtítulos rotos"* medidos sobre `output/video_001_final.mp4`, cuyo `mtime` es **12:47:49** (commit `13aec78`), cuando el tratamiento de la ventana aplastada (ANCLA-04) entró en `a7766ac` a las **13:29** — se midió un binario **de otro commit** y parte del defecto ya estaba arreglado · lo cazó el autor del SEED revisando el veredicto del revisor, no el revisor · evidencia: `sed -n '288,300p' modules/shorts_generator.py`; `ffprobe` sobre `shorts_tiktok/short_001.mp4` → 11.59 Mbps; `os.path.getmtime('output/video_001_final.mp4')` = `2026-08-11 12:47:49` contra `git log --date=format:'%H:%M' -- modules/tts_engine.py` → `a7766ac 13:29` · es la misma clase que [INSTR-04] (comparar producción contra el baseline del fixture de 3 min) vista desde otro ángulo: **la métrica estaba bien, el punto de comparación no** · test barato que los habría evitado: contrastar el `mtime` del artefacto contra el `git log` del módulo que lo produce, y comprobar de qué formato es el umbral antes de llamar defecto a un número · estado: pendiente (>=2)

- [COMA-03] 2026-08-11 · cadena-causal-mal-atribuida · **[COMA-02] atribuyó a la densidad de comas tres síntomas y solo uno es suyo.** Descomponiendo `duración = fonación + silencio` sobre los dos `.ass` reales: la voz **articula igual** en ambas corridas (236,2 → 242,3 wpm por tiempo de fonación; ajustando por ventanas de `SentenceBoundary`, incluso al revés: 227 vs 208). De los 208 s de diferencia entre las dos producciones, **187,3 s (90%) son silencio y ~22 s (10%) velocidad**; y del silencio, el término dominante son los **puntos** (232,2 → 128,9 s, 1,29 s por frase) por encima de las **comas** (117,8 → 56,0 s, 0,25 s cada una): un punto vale **5,9 comas**, atribución 63%/38%. Corolario: `_ensure_breathing_commas` solo puede insertar comas, así que ataca el término menor · **el objetivo "8-9 comas/100" es además aritméticamente inalcanzable**: techo medido del inserter con los umbrales a **1** = `298 comas = 5,59/100` (harían falta 453), y solo hay **397 conectores** en todo el texto, de los que 309 son `y` y casi todos los rechaza `coordina_sintagma` · **el ratio no se cierra por esta vía**: contrafactual con el coeficiente medido (+0,248 s/coma) → 453 comas compran **+76 s**, ratio 0,79 → 0,83, aún bajo el umbral 0,90 de `audit_run.py:135`; y la referencia "10-ago ratio 1,00" era **falsa** (1790,9 s / 2004 s = **0,894**, que ya suspendía ese mismo umbral), así que el efecto atribuido a las comas estaba **duplicado** · el repo ya tenía el A/B controlado y decía esto: `docs/mediciones-frases.md` §A — añadir comas mueve la velocidad **207 → 204 wpm (1,5%)**, acortar las frases la mueve **207 → 172 (17%)** · lo que **sí** sobrevive de [COMA-02], normalizado: pausas fuera de puntuación **0,72% → 2,00%** de los sitios no puntuados (2,8x peor), contrastado con `silencedetect -35dB` sobre el audio real (333 silencios/216,9 s vs 325/238,2 s derivados del `.ass`, concuerdan al 2,5%); y que `target_wpm` no es calibrable, por una razón mejor que la que daba [COMA-02] · **palanca determinista que nadie había nombrado**: `main.py:387-388` hace `os.remove(chunk_path)` sobre el chunk entero, así que los 422 s de gameplay no usados **se destruyen** en vez de volver a `pool/`, cosa que `take_chunk` ya sabe hacer (`gameplay_pool.py:206-209`) · estado: pendiente (>=2)

- [ANCLA-05] 2026-08-11 · alineador-fabrica-el-interior · Tras [ANCLA-04] quedaba **una ventana por corrida** con los subtítulos ~1,45 s por detrás de la voz, y **no era la clase que el SEED suponía** ("la ventana entera está desplazada"): el ancla acierta a **−0,010 s** contra la transcripción independiente. Lo inservible es el **ritmo de dentro**. Firma medida (11-ago, ventana t=128,55): **26 de 29 palabras con duración IDÉNTICA de 0,200 s** (90% = relleno de stable-ts, no una medida) y dos huecos internos inventados de **2,82 s y 2,62 s**; el cierre de huecos recorta el exceso pero **retiene `ANCLA_HUECO_MAX`=0,80 s por hueco** = 1,60 s de silencio inexistente que empuja tarde a las 27 palabras siguientes · ningún guardia podía cazarlo: 143 wpm queda muy por debajo de `ANCLA_WPM_MAX`=330, que mide el lado **contrario** (la ventana aplastada), y la guarda de ancla corrupta hace bien en no tocarla · evidencia: `raw_11ago.json` ventana 11 → `span crudo 133,18→145,36 = 12,18 s` frente a `dur=9,28 s` de edge-tts (ratio 1,31), duración modal 0,200 s en 26/29, los dos huecos con su retención de 0,80 s cada uno · arreglado con un detector *uniformidad ≥50% **Y** ≥1 hueco inventado* (marca **2 de 268** ventanas de las dos producciones, las 2 realmente rotas, 0 falsos positivos) enrutado a la rama de reparto por caracteres **que ya existía** — sin camino nuevo y **sin reescalar** la ventana estirada, que está medido y descartado · A/B propio contra transcripción independiente con emparejado local: 11-ago **+1,450 → +0,129 s**, malas 2→1, >0,5 s tarde 99→76, p95 0,287→0,239; 10-ago **+0,507 → −0,068 s**, malas 4→3; **0 ventanas empeoran** en ninguna de las dos; invariantes intactos (nº de palabras, orden monótono, fin del título **Δ 0,0000 s**) · **el mismo defecto ya estaba en la corrida del 10-ago, más pequeño y sin detectar** · pendiente: el gemelo comparte el código (`shorts_generator` → `run_tts`) pero **no hay banco para shorts** porque no se guarda su alineación cruda, así que la mejora allí está INFERIDA, no medida · estado: pendiente (>=2)

- [GATE-02] 2026-08-11 · gate-ciego-por-construccion · `scripts/audit_run.py` y `scripts/eval_sync.py` miden **DESFASE** emparejando subtítulo con transcripción, así que **un subtítulo que no existe no genera par y es invisible con cualquier umbral**. En la corrida del 11-ago eran **8,5 s seguidos de narración con la pantalla en blanco** (t=142,80-151,31, de los cuales ~6,65 s con voz sonando) y el gate lo daba por bueno. No es un umbral mal puesto: es que la magnitud medida no puede expresar el defecto — hace falta una métrica de **cobertura**, no de desfase · segundo caso de la misma familia: `_enforce_monotonic` producía **28 palabras en 1,40 s** (1200 wpm en pantalla, ilegible) y lo dejaba en un `WARNING` que nadie lee, sin que ninguna métrica lo mirara · evidencia: `voz_sin_subtitulo` sobre el vídeo publicado → **8,30 s en 6 tramos, peor 5,05 s en t=145,2**; `rachas_aplastadas` → **racha máxima 28 en t=139,2**; ambos sobre artefactos que el gate ya tenía delante · añadidas 5 métricas (cobertura con `silencedetect`, rachas en el suelo, cierre narrativo desde el log, longitud de título contra los 100 caracteres de YouTube, arranque mutilado de shorts), todas **calibradas contra el caso conocido antes de usarlas** · trampa pagada al construirlas: la primera versión del detector de arranque mutilado medía sobre el `.ass` y daba **0 de 14** en un corpus donde el defecto existe, porque el `.ass` de un short arranca **después** del título (`skip_until`) y la cola mutilada está en la posición 0 — un instrumento nuevo que da cero en verde es sospechoso, no tranquilizador · `audit_run.py` **sigue sin estar cableado** a `main.py`/`dashboard_runner.py`: es un script suelto que hay que acordarse de correr, y la corrida del 11-ago murió antes de que nadie lo corriera · estado: pendiente (>=2)

- [TITULO-01] 2026-08-11 · guardia-por-prefijo-demasiado-estricta · **7 de 48 shorts del corpus (2 de los 14 de la última corrida) empiezan narrando un fragmento MUTILADO de su propio título**, justo después de decirlo — en un short vertical eso es el segundo 1, o sea el producto entero. Caso real: título *"…Vació Mi **Carteras Fría** Y Desapareció Con Mis Bitcoins."* seguido de *"**cartera fría**, y desapareció con mis bitcoins. Compartí la frase semilla…"* · dos redes fallaban a la vez en `_ensure_title_at_start`: (a) `overlap` es un match por **prefijo palabra a palabra**, así que rompe en la primera palabra distinta y una paráfrasis del modelo ("Mi padrino **de bautismo** vendió…") deja la cola entera pegada; (b) la segunda red exigía que el fragmento fuese **subcadena literal** del resto del título, y **una sola letra la rompía** (`carteras` contra `cartera`), con lo que hacía `pass` y lo conservaba · arreglado comparando por **solape de tokens** (`difflib` sobre palabras normalizadas, umbral 0,60) y corriendo la segunda pasada SIEMPRE, no solo cuando el prefijo comió algo · verificado: 2/2 shorts rotos arreglados, **0 falsos positivos** en los 12 sanos, historia larga de 5305 palabras intacta, 6 casos degenerados sin excepción · **confirmado en generación fresca**: en la corrida de `/eval` del 11-ago el log registra `Eliminado eco del titulo al inicio: 'moría en el hospital pero no sabía que yo había grabado la coacción y '`, y los 4 shorts nuevos salen limpios · el auditor lo mide desde ahora; ojo: su primera versión medía sobre el `.ass` y daba **0 de 14** en un corpus donde el defecto existe, porque el `.ass` de un short arranca **después** del título (`skip_until`) · estado: pendiente (>=2)

- [TITULO-02] 2026-08-11 · campo-externo-nunca-comprobado · Los títulos de este pipeline son de 28-38 palabras y **150-191 caracteres**; el campo de título de YouTube corta en **100**. Nadie lo había decidido: se descubría por truncado, y lo que se publicaba perdía el gancho entero, que en este estilo va al FINAL (*"…Ante La Notaría Pero No..."*) · **nunca se comprobó contra el límite real de la plataforma** aunque el uploader lleva desde el 11-ago recortando a 100 "por si acaso" · arreglado separando dos campos (`_title.txt` largo, que se sigue narrando y va a intro y miniatura; `_title_yt.txt` corto para YouTube) · **el fallo gordo apareció al probar contra la API REAL, no en las pruebas con mock**: con `max_tokens=200` sobre `nvidia/nemotron-3-ultra` —que es un modelo de RAZONAMIENTO— los **5 de 5** intentos devolvieron razonamiento cortado justo en el tope (599-745 caracteres, empezando por *"The user wants a short title…"*), el título no llegaba a escribirse nunca y la función **siempre** caía al fallback: 5 peticiones gastadas para acabar en el mismo truncado que ya hacía el uploader. La clase estaba documentada en `CLAUDE.md` desde agosto y se repitió igual · arreglado con `max_tokens=2000` y quedándose con la **última línea útil**, que es donde un modelo que razona pone la respuesta · medido tras el arreglo: `172 → 'Mi Hermano Falsificó El Testamento Pero Grabé Su Confesión'` (58 chars) y `191 → 'Mi Hermano Me Obligó Firmar Renuncia Herencia Grabé Coacción Juez Anuló Todo'` (76), los dos conservando el gancho · **lección de método**: dos subagentes verificaron con `_call_openrouter` monkeypatcheado y ambos dieron verde; el mock no puede ver que el modelo no responde lo que se le pide · coste: +1 petición por vídeo largo (~55-61 → ~56-62 de 1000/día) · el gemelo: **4 de 30 títulos de shorts pasan de 100 caracteres**, se avisa pero NO se tocan porque se narran · estado: pendiente (>=2)

- [MAIN-01] 2026-08-11 · verde-local-no-funciona · El cableado del auditor a `main.py` reventó **en ejecución** con `NameError: name 'logger' is not defined`, justo después de `Pipeline finalizado`, con `compileall` limpio, el dashboard arrancando sin excepciones y los tests de la cola de subida en verde. En `main.py` **`logger` es local a cada función** (`logging.getLogger(__name__)` dentro de cada una), no de módulo, y la función nueva no lo declaraba · agravante: la línea que petó (`logger.info` del comando) estaba **fuera** de su propio `try`, así que rompía la garantía "marcar, no matar" que ese mismo código venía a implementar — la corrida de 21 min terminó con traceback · lo cazó `/eval`, no la revisión ni la compilación · evidencia: `pipeline.log` de la corrida del fixture, traceback completo tras `15:57:40 Pipeline finalizado: 1 videos producidos` · arreglado declarando el `logger` y metiendo **todo** el cuerpo dentro del `try`; probado con 4 configuraciones degeneradas (config sin `paths`, `paths` incompletos, `args` sin el atributo, directorios inexistentes) y ninguna propaga · estado: pendiente (>=2)

- [GATE-03] 2026-08-11 · gate-no-determinista · **El fixture de `/eval` genera una historia NUEVA en cada corrida**, así que su comparación contra el baseline no puede distinguir un cambio de código de otra tirada del modelo. Medido: baseline (video_003) 3,85 comas/100 y frase máxima de 52 palabras; corrida nueva (video_004) **1,84 comas/100 y frase máxima de 106**. Con eso, sincronismo medio 0,043 → 0,083 s, máximo 0,32 → 0,50 y pausas fuera de puntuación 0 → 10 — que por la letra de la regla del gate es **BLOQUEA**, y no lo causó ningún cambio de código · desambiguado con dos medidas: (a) el detector de [ANCLA-05] **disparó 0 veces** en las 5 alineaciones, y como `ritmo_inservible = aplastada or fabricada`, sin disparos el comportamiento es idéntico al anterior; (b) **A/B controlado**: re-alineando la historia DEL BASELINE con el código de hoy la diferencia es **media 0,0053 s, p95 0,0200 s** sobre 465 pares · consecuencia práctica: el gate sirve para detectar que la cadena entera corre y para los umbrales ABSOLUTOS, pero su comparación relativa contra baseline necesita un A/B controlado (misma entrada, dos códigos) antes de llamar regresión a nada · estado: pendiente (>=2)

- [AUDIT-01] 2026-08-12 · veredicto-caducado-certifica · `output/video_002_audit.json` decía `{"ok": true, "fallos": []}` sobre el vídeo del que Diego se quejó de oído, y el dashboard lo ofrecía para **subir a YouTube**. El JSON es de las **16:43**; `scripts/audit_run.py` se reescribió a las **17:13** y el guardia de puntuación a las **17:30**: el veredicto se emitió con un auditor que ya no existía y **no caducaba al cambiar los criterios** · el SEED de la sesión siguiente usaba ese mismo veredicto como su evidencia principal ("sin defectos MEDIBLES, el primero limpio") · evidencia: `ls --time-style=full-iso output/video_002_audit.json scripts/audit_run.py modules/script_generator.py` · arreglado con `utils.huella_auditor()` (sha256 de `audit_run.py`+`eval_sync.py`) escrita en el veredicto y comprobada en `lee_veredicto`; se eligió hashear el fichero y no un `VERSION` a mano porque un número que hay que acordarse de subir es una garantía en prosa (§17) · verificado: `lee_veredicto('output/video_002_final.mp4')` → `ok=False, caducado=True` · de paso, la vía de recuperación del dashboard estaba rota por las **dos** mitades: mandaba correr `audit_run.py --keep-temp` (flag que su argparse no acepta) y decía que borrar el JSON desbloquea, cuando `lee_veredicto` devuelve `ok=False` si el fichero no existe · estado: pendiente (>=2)

- [COMA-04] 2026-08-12 · guardia-mide-el-artefacto-equivocado · El guardia de puntuación del 12-ago (`_validar_puntuacion`) juzgaba la densidad de comas del guion **CRUDO**, que el pipeline reescribe aguas abajo con `_ensure_breathing_commas`. Medido: 0,02 y 0,19 comas/100 en crudo son **3,11 y 2,74 NARRADAS** — las dos corridas "malas" ya pasaban el umbral de 2,5 en la forma que de verdad se oye, así que el guardia rechazaba y reintentaba (~12 peticiones/vídeo) guiones cuya forma hablada ya cumplía · y la métrica **no predice el defecto**: con 10x más comas crudas (11-ago vs 12-ago) las pausas inventadas medidas acústicamente fueron **102 vs 86**, indistinguible · el hallazgo ya estaba en el ledger: **[COMA-03] del día anterior** midió la atribución 63% puntos / 38% comas y que "8-9 comas/100 es aritméticamente inalcanzable", y el fix del día siguiente atacó las comas igualmente — el ledger existía y no se consultó al diseñar · arreglado cambiando a una métrica ligada al mecanismo (edge-tts respira cada ~20 palabras): **p90 de palabras entre signos sobre el texto NARRADO**, máximo 30; las dos historias reales pasan (0 peticiones extra) y un texto de 400 palabras sin conectores falla · estado: pendiente (>=2)

- [BASURA-02] 2026-08-12 · basura-del-modelo-por-la-COLA · El modelo terminó la historia anotándose a sí mismo — `"...que pienso defender hasta el final.PALABRAS: 1558"` — y eso **se narró y se subtituló**: el último cue del `.ass` del fixture era literalmente `1558.`, con la voz diciéndolo. `_detectar_basura` devuelve `False` y **no puede verlo**: exige una ráfaga de ≥3 tokens anómalos DISTINTOS en 60 palabras y esto son dos, uno de ellos un número limpio · tercera posición de la misma clase: [BASURA-01] fue el **cuerpo**, los guardias originales miraban la **cabecera**, y este es la **cola** — cada vez que se tapa un sitio el modelo aparece por el siguiente (§17, 2.º corolario) · lo cazó `/eval`, no la revisión ni `compileall` · evidencia: `temp/video_002_story.txt` últimos 260 caracteres; `test_e2e/temp/video_002_subs.ass` último cue `('1558.', 178.89, 179.34)` · arreglado con `_strip_trailing_metadata` aplicado ANTES del truncado (si va después, el metadato cuenta como palabras del objetivo y el truncado conserva justo la cola donde vive) y propagado al gemelo de shorts; 5 variantes cazadas, 4 controles legítimos intactos incluido `"Mi madre nació en 1558."` · estado: pendiente (>=2)

- [SUBT-01] 2026-08-12 · fix-que-mueve-otra-metrica · Partir las frases en código (`_ensure_breathing_periods`) **duplica el número de ventanas de anclaje** —en el fixture 13 → 28— y con ellas los bordes donde el subtítulo puede llegar tarde. Medido sobre las dos corridas del fixture: **voz sin subtítulo 0,00 s → 1,60 s en 3 tramos**, y los **3** caen inmediatamente después de una palabra que cierra frase (`LEGADO.` 76,40; `TIMBRADO.` 150,71; `1558.` 179,49), que es la firma del mecanismo · el gate lo marcó `FALLA` (umbral: total < 1,0 s) · extrapolación a producción, que es lo que preocupa: video_002 pasa de 98 a 209 frases con el partidor, así que a la tasa medida (0,057 s por ventana) serían ~12 s de voz sin subtítulo en un vídeo de 26 min — el defecto [GATE-02] del 11-ago era de 8,3 s · causa probable no confirmada: la palabra final de frase se alarga por prosodia (final lengthening) y su cue termina antes de que la voz acabe de decirla · causa confirmada: al cerrar frase el TTS **alarga** la última palabra (*final lengthening*) y el alineador le da la duración de siempre, así que el cue se va de pantalla mientras la voz aún la dice; el partidor no creó el defecto, multiplicó sus sitios · arreglado con `_extend_sentence_final_words`, que alarga el cue **solo dentro del tramo de voz** medido con `silencedetect` (instrumento independiente del alineador, que es el que se equivoca): si ya empezó el silencio no se toca, y así se preserva que los subtítulos desaparezcan en las pausas · A/B controlado sobre los artefactos reales del gate (misma entrada, dos códigos, como exige [GATE-03]): **1,60 s en 3 tramos → 0,00 s en 0**, `starts` **todos** intactos (el sincronismo se mide sobre `starts`: no se mueve), **0** solapes, fin del título **17,97 → 17,97 s** (la intro no se mueve), 25 cues alargados de 533 · trampa evitada: la primera validación usó el audio del **vídeo final**, que lleva −100 ms de offset; repetida por el camino real (el MP3 del TTS) da lo mismo · la función necesitaba dos imports nuevos (`subprocess`, `_find_exe`) que **`compileall` no habría cazado** — es la clase [MAIN-01], y se cazó con `pyflakes` + una ejecución real de `run_tts` · estado: pendiente (>=2)

- [LLM-01] 2026-08-12 · camino-que-nadie-ejercia · El `/eval` murió con `'NoneType' object has no attribute 'strip'` tras 6 min, sin producir vídeo. Causa raíz (`script_generator.py:109`, validada por ejecución con la capa HTTP falseada, 0 peticiones): `choices[0].get("message", {}).get("content", "").strip()` — **`.get(k, "")` solo devuelve `""` si la clave NO existe**; OpenRouter manda la clave con valor **`null`** cuando el modelo de razonamiento **agota el presupuesto razonando** y no llega a escribir la respuesta (200 OK, `content: null`, el texto en `reasoning`, `finish_reason: "length"`). `None.strip()` lanza un `AttributeError` que **ningún `except` del bucle atrapa** (solo cazan `RequestException` y `ValueError`), así que salía disparado del reintento y mataba el vídeo entero **con 1 sola petición gastada** · la línea llevaba **byte a byte igual desde el commit inicial** (`git log -S` → `db338bb`): no es regresión, es el camino que nadie ejercía (§19), y el guardia de "200 sin `choices`" que sí existe cubre el cuerpo **sin** `choices`, no el cuerpo **con** `choices` y `content: null` · **la firma del log fue lo que lo discriminó**: cero WARNING entre el inicio del bloque y el error, lo que descarta todos los escenarios que pasan por el reintento (cadena vacía, una sola línea, 429/5xx) y prueba que hubo **una sola** petición de 360 s — coherente con que el intento más largo posible es justo el que agota el tope de tokens · el gemelo también lo heredaba: `_call_openrouter` es la costura única, y en `shorts_generator.py` (que no tiene ni un `except`) el fallo era **mudo** — `main.py` atrapa por short y sigue, así que desaparecía un short de la tanda sin que ninguna métrica lo marcase · arreglado con extracción None-safe que encauza el caso al guardia de reintento que ya existía, y con un `last_error` que **distingue** "gastó el presupuesto razonando" de "devolvió un cuerpo de error", porque un reintento idéntico no arregla lo primero (lo arregla `max_tokens`, como ya hace `generar_titulo_youtube`) · verificado con 6 payloads degenerados (content null con y sin reasoning, message null, choices vacío, cadena vacía, solo espacios): **todos** acaban en `RuntimeError` legible, **ninguno** en `AttributeError`, camino feliz intacto en 1 petición · **coste**: este caso pasa de 1 petición a hasta 5, y un intento puede tardar ~6 min (medido 301 s y 360 s) · agujero de diagnóstico tapado de paso: `main.py` registraba solo `str(e)` sin traza, así que averiguar la línea costó una investigación entera con repro monkeypatcheado → ahora `exc_info=True` · estado: pendiente (>=2)

- [INSTR-05] 2026-08-12 · metrica-agregada-esconde-el-defecto-local · La comprobación acústica que le puse hoy al auditor (`pausas_inventadas`) es un **NETO**: `exceso = nº silencios − nº signos`, y por eso es libre de emparejador. El precio, que no vi al diseñarla: **cuenta, no localiza**. Con `_ensure_breathing_periods` metiendo el doble de signos (283 → 312 en la historia de producción), el neto se va a **0 aunque haya pausas mal puestas**, porque los signos de sobra compensan los silencios inventados. Medido en la corrida verde del gate: la métrica acústica da `0 = 0,0 por 1000` (PASA) mientras el instrumento **posicional** sobre el `.ass` marca **6** huecos fuera de puntuación, uno de **1,09 s tras "cambió"** en mitad de la oración *"…porque cambió de apellido recuperando el de soltera…"*, que es exactamente el defecto que Diego describió como "pausas en sitios que no debería" · o sea: **el gate está en verde y el defecto de clase sigue existiendo**, solo que más pequeño · es la misma familia que "la mediana esconde el defecto local" ([ANCLA-01]: una zona con mediana −0,110 s contenía 40 palabras a −7,4 s), y me la he vuelto a comer construyendo el instrumento, no leyéndolo · el instrumento posicional que sí lo ve es **circular** (mide el `.ass`, que es la alineación que se juzga), y el emparejador por reloj de habla que lo haría no-circular **falló su calibración al 8%** sobre 26 min · pendiente: una medida **posicional y no circular** — emparejar los silencios con el texto usando una transcripción independiente por tramos cortos (calibrada al 98-100% sobre 420 s en esta misma sesión) en vez de sobre el audio entero · estado: pendiente (>=2)

- [LOOP-01] 2026-08-13 · afinar-la-constante-en-vez-de-arreglar-el-mecanismo · Moví `PALABRAS_FRASE_MAX` **tres veces en una sesión** (12 → 40 → 30) y en cada una descubrí que la métrica con la que había elegido la anterior estaba incompleta: la 1.ª por [SYNC-01] (la variable manipulada dentro del instrumento de selección), la 2.ª por mirar solo el audio y no el sincronismo, la 3.ª al ver que el barrido **no es monótono**. Contradije por escrito mis dos recomendaciones anteriores ante el usuario · el fallo de fondo NO es la calibración: es que estaba **moviendo un parámetro para esquivar un bug** en vez de arreglarlo — la ventana aplastada del alineador (`Ventana aplastada en t=110.14s: 57 palabras en 10.32s = 331 wpm`, con el ancla descartada por residuo −4,742 s). `cada=30` no la cura, tiene suerte con dónde caen los cortes · es `decision-making.md` §7 (*challenge-the-premise*) en su forma más cara: afinar constantes cuando el mecanismo es el equivocado · evidencia de que la constante no es la causa: A/B controlado sobre el mismo texto con 4 ajustes contra transcripción independiente da peor tramo −0,213 / +0,421 / −0,198 / +0,415 para cada=12/20/30/40 — **no es una escalera**, así que la longitud de frase no explica el desfase · **lo cortó Diego, no yo**, con la señal que `CLAUDE.md` §6 dice que hay que mirar ("si el contexto es muy largo y empiezas a repetir errores, sugiere sesión fresca"): *"¿y si miramos qué sucede desde sesión fresca por contexto? No creo que sea tan difícil hacerlo bien"* · yo había escrito la frase correcta ("si vuelve a salir rojo, el problema no es la constante, es el alineador") y **aun así ofrecí otro barrido** · traspasado a `seeds/SEED_alineador_ventana_aplastada.md` · estado: pendiente (>=2)

- [PARTIR-01] 2026-08-13 · exencion-mal-ubicada · `_ensure_breathing_periods` (escrito ayer) inicializa `titulo_en_curso = True` **dentro** del bucle `for parrafo in text.split("\n")`, así que la exención pensada SOLO para el título forzado se aplica a **la primera frase de CADA párrafo**. Reproducido: un párrafo formado por una sola frase larga **no se parte ni con `cada=12`** · consecuencia medida: la frase de 57 palabras que se convirtió en la ventana aplastada del alineador (y que se comió 16 s de vídeo con 21 palabras >0,5 s tarde) **es la primera de su párrafo y mide 57 palabras con `cada` = 12, 20, 30 y 40 por igual**; `video_003` arrastra otra de **181** · **esto reexplica el barrido A/B de ayer**: la sesión anterior concluyó "unos cortes parten la zona patológica y otros no" cuando la verdad es que **ningún valor de la constante la toca** y las diferencias de aquella tabla eran ruido de las otras frases — misma conclusión (no tocar la constante), razón correcta · lo destapó un panel adversarial sobre el SEED, no la medición que eligió la constante: el instrumento con el que se calibró (frase mediana y p90 del texto completo) **no puede ver una exención sistemática**, porque las frases exentas siguen contando en la distribución · evidencia: `_ensure_breathing_periods` con dos párrafos y `cada=12` deja el segundo íntegro; distribución de `video_004_story.txt` con cada=12/20/30/40 → max **57** en los cuatro · **NO arreglado**: `modules/tts_engine.py` pertenece al track A (`SEED_alineador_ventana_aplastada.md`), que lo lleva en su alcance · **ARREGLADO el 13-ago** en el track A: `titulo_en_curso` sale del bucle de párrafos; invariante de palabras verificado en 3 corpus (604/5290/5334 sin cambio) y la frase de 57 baja a 43. **No cierra por sí solo la ventana aplastada** (43 palabras siguen dando ~331 wpm y siguen disparando `aplastada`): lo que hace es devolver `ANCLA_WPM_TIPICO` a su régimen de validez, ver [ANCLA-06] · estado: pendiente (>=2)

- [SEED-01] 2026-08-13 · seed-escrito-desde-dentro-del-ancla · La v1 de `SEED_alineador_ventana_aplastada.md` la escribió la misma sesión que produjo el diagnóstico, y un panel adversarial la reescribió entera. Cuatro defectos, todos verificados: (a) mandaba **investigar** una causa que ya se podía **cerrar** con los artefactos en disco (3-5 h redundantes); (b) daba como criterio de aceptación `anchor_bench` con "0 ventanas que empeoran" cuando ese banco es **ciego a este fallo** — en los dos corpus del banco `util` lo limita el tope de 210 wpm y solo en `video_004` lo limita `s_dur`, así que un fix podría dar verde **sin tocar el caso que rompe**; (c) afirmaba que "los artefactos están en disco" cuando las **entradas** del anclaje (palabras crudas de stable-ts + `SentenceBoundary`) **no se persisten en ninguna parte**, así que el caso NO es re-corrible offline; (d) **no llevaba cláusula de alcance**, siendo el único de los cinco seeds sin ella — y `cleanup_temp` hace `rmtree` de `test_e2e/temp/`, que habría **borrado la única evidencia física del bug** · agravantes que el SEED tampoco decía: `anchor_bench dump-raw` con sus **defaults** escribe en `data/evidence/` (declarado INTOCABLE) y destruye el corpus del 10-ago; y `video_004` se produjo con el commit `27f025e`, no con HEAD, que es la clase [REVIEW-01] · **lo pidió Diego** ("revisa bien el SEED... siendo 100% objetivo"), no salió de la sesión · estado: pendiente (>=2)

- [ANCLA-06] 2026-08-13 · garantia-en-comentario-no-implementada · La rama de reparto por caracteres de `_validate_and_fix_alignment` llevaba encima el comentario *"Sobre la duración de la VOZ, no sobre la ventana entera: la ventana de edge-tts incluye el silencio posterior"* y **la línea de debajo no lo implementaba**: `util = min(s_dur, len(indices)/ANCLA_WPM_TIPICO*60)`. El tope crece **linealmente con n** y el silencio de la ventana no, así que solo muerde si el silencio supera ≈`0,025·n` s: para n=57 hacían falta 1,42 s y solo había 1,13 → `util` degeneraba en `s_dur`, la ventana ENTERA. **El guardia era inerte justo en las ventanas largas, que son las que más daño hacen** · medido en `video_004` (t=110,13, 57 palabras, 16,0 s de vídeo) contra transcripción independiente: mediana **+0,460**, max **+0,911**, **21 palabras >0,5 s tarde — las 21 de toda la corrida**. Es el `peor tramo +0.610s en t=115.81s` que tumbó el gate (Δ +0,100 s = el `-itsoffset -0.10` de `video_composer.py:88`: los números del MP3 y del MP4 NO son los mismos) · **el mecanismo tenía DOS componentes y el diagnóstico inicial solo veía uno**: el error no crecía monótono sino en **diente de sierra con tres reinicios**, y los tres caían en los tres silencios internos medidos (1,196 s en total) que el reparto plano aplanaba. Por eso cambiar el escalar `util` era la mitad del fix: el mejor escalar dejaba residuo `min −0,68 / max +0,45` ≈ la pausa ignorada · arreglado repartiendo sobre los **tramos de voz medidos** (`silencedetect`, instrumento independiente del alineador), caminando por un "reloj de voz" que colapsa los silencios; `run_tts` mide los tramos **una sola vez** y los comparte con `_extend_sentence_final_words` (coste extra 0) · **alternativa medida y REFUTADA**: mapear afínmente el ritmo crudo de stable-ts sobre el span de voz da mediana **+7,6 s** — el ritmo crudo de una ventana aplastada es basura (residuo −4,742 s), conservarlo conserva el error. [ANCLA-04] había descartado el reescalado para el caso contrario (ventana que ocupa MÁS de lo que se tarda); ahora está descartado también para éste · verificado por ejecución en los **tres** corpus del banco, con el control del instrumento en 0,0027-0,0047 s: v004 ventanas rotas **1 → 0**, palabras >0,5 s tarde en la ventana **22 → 0** y **0 pronto** (sin sobrecorrección), p95 global **+0,391 → +0,119**; 11-ago **2 → 0**; **0 ventanas que EMPEORAN** en los tres · **guarda dura**: el fix toca SOLO las ventanas que entran en la rama — 1 de 18 en v004 y 2 de 110 en 11-ago, con las otras **547 y 5261 palabras idénticas bit a bit** · **lo que NO alcanza y se dice**: `|err| max` dentro de la ventana queda en **0,443 s** frente al objetivo declarado de 0,30, y la mediana en +0,119 frente a +0,10; ningún mecanismo probado llega ahí, y no se ha tuneado la constante contra el propio instrumento ([SYNC-01]). Además la ventana pasa de 0,00 a **0,39 s** de voz sin subtítulo, efecto del recorte del cue en el borde del silencio (que es la especificación: el subtítulo desaparece en las pausas) · sin audio **no hay fallback mudo**: reparte como antes, lo **cuenta** y lo grita en el log, y el banco lo declara no evaluable — el 10-ago no tiene audio conservado · estado: pendiente (>=2)

- [BASURA-03] 2026-08-13 · basura-del-modelo-por-la-COLA (3.ª posición de la misma clase) · `video_004` **narró y subtituló el índice en Markdown que el modelo escribió sobre sí mismo**: `**Cuenta final de palabras: ~1.598 palabras** (Perfecto, objetivo alcanzado). **Resumen de los elementos solicitados incluidos:** 1. **Plan y ejecución:** …`, del cue 449 (`0:02:31.66 RESUMEN / DE / LOS / ELEMENTOS / SOLICITADOS`) al último (`0:03:27.45 … AL CORTARSE BIEN.`) = **55,8 s de 208 = 27% del vídeo** · los tres guardias en verde: `_strip_trailing_metadata` **no-op** (604 → 604 palabras), `_detectar_basura` `(False, '')`, y el auditor `OK basura del modelo: ninguna`. `_RE_META_FINAL` solo cazaba `PALABRAS: N` / `FIN DE LA HISTORIA` — [BASURA-02] tapó **una forma** de la cola y el modelo salió por la de al lado, exactamente como [BASURA-01]→[BASURA-02] (§17, 2.º corolario) · **agravante que encadena con [ANCLA-06]**: `_truncate_to_words` hace `cuerpo + "\n\n" + cierre` y **ascendió esa cola a "desenlace"**, tirando 1.540 palabras de narración real (`pipeline.log`: *"se descartan 1540 palabras del CUERPO (frases 11-46 de 55). Se conservan las ultimas 256"* — y los dos últimos párrafos suman exactamente 256); ese `\n\n` **fabricó el párrafo cuya primera frase de 57 palabras es la ventana aplastada**. La cadena causal del bug del alineador empieza aquí · **no tenía dueño en ningún track**: lo destapó el panel adversarial del `/seed-review`, y el SEED lo declaraba "fuera de alcance, lo cubre otro track" · arreglado con `_detectar_meta_cola` (cabecera markdown de auto-análisis + lista numerada con sub-cabeceras en negrita), corte en el último fin de frase **antes** del marcador, backstop que **aborta** en `_truncate_to_words` en vez de ascender basura a desenlace (§12), y el auditor midiendo la **cola** · verificado por ejecución: `video_004_story.txt` **604 → 472** palabras cortando en *"…no se astilla, se fortalece."* y siendo **prefijo exacto** del original; **0 falsos positivos** en los dos corpus de historias buenas (5290 → 5290 y 5334 → 5334); una lista numerada legítima sin negrita **no** se caza · el gemelo de shorts lo hereda por importar la misma función · estado: pendiente (>=2)

- [INSTR-06] 2026-08-13 · corpus-historico-reconstruido-con-codigo-de-HOY · Al construir el tercer corpus del banco, el agente concluyó que *"con HEAD la ventana de 57 palabras desaparece (sale 43+14) y el control falla con 0,28 s"*, y escribió esa conclusión **dentro de `anchor_bench.py`** como justificación del commit elegido. **Es falsa**: cargando los dos commits con `git show`, `27f025e` (PALABRAS_FRASE_MAX=40) y `d1496a8` (=30) dan **los dos** la misma frase de 57 palabras — con el bug de [PARTIR-01] presente la frase estaba exenta del partidor con CUALQUIER valor. El "43+14" salió de **importar el módulo en caliente** mientras el working tree ya tenía aplicado el fix de `titulo_en_curso` de esta misma sesión: el corpus "histórico" se estaba reconstruyendo con código del futuro · no invalidó el corpus (el control con `27f025e` pasa a 0,0027 s y `27f025e` es además el commit real por cronología del `pipeline.log`), pero sí su razón, y la razón es lo que se lee después · es [REVIEW-01] con el signo cambiado —comparar contra el binario equivocado— y la clase T1 de las reglas transversales: **un dato escrito en un fichero se propaga como verdad**; aquí iba a quedar dentro del propio instrumento · corregido en el comentario de `scripts/anchor_bench.py`, con la regla operativa: al reconstruir un corpus histórico, carga el código con `git show`, nunca `import` a secas · estado: pendiente (>=2)

- [META-01] 2026-08-14 · guardia-que-destruye-lo-que-protege · `generate_story` llamaba a `_strip_trailing_metadata` **una sola vez sobre la historia YA CONCATENADA** de 3-6 bloques (`script_generator.py:1062`). `_detectar_meta_cola` devuelve `min(posiciones)` —el marcador MÁS TEMPRANO de todo el texto— y el corte va desde ahí **hasta el final de la cadena**. Correcto para UN bloque (así se midió [BASURA-03]); destructivo sobre la concatenación: la auto-anotación al final del bloque 1 se lleva por delante todos los bloques siguientes, **incluido el desenlace que [CIERRE-01] acababa de garantizar**. El único aviso era un `logger.warning` con la historia borrada dentro (§12) · evidencia: A/B con entrada idéntica y dos códigos sobre `data/evidence/video_001_story.txt` (5307 palabras) con el marcador literal de `video_004`; marcador en bloque 1 → HEAD **1300 palabras** (76% borrado, acaba a mitad de escena en *"Le pregunté por qué."*), con fix **3900** y desenlace intacto; marcador en bloque 2 → 2595 vs 3895. En los dos casos `_detectar_meta_cola` sobre el guion resultante da **limpio**, así que el auditor no puede verlo. No regresión: sin marcador, sha256 **idéntico** (`4b5995f380824bdf`) · arreglado: limpieza POR BLOQUE (`_limpiar_bloque`, aplicada al bloque 1, a cada continuación y al cierre ANTES del mínimo de palabras) + backstop con dientes en la concatenación (`_META_CORTE_MAX_FRAC = 0.15` → `raise`, no warning) · estado: pendiente (>=2)

- [LIMPIEZA-01] 2026-08-14 · borrado-silencioso-invisible-a-toda-metrica · `_clean_speech_for_tts` tenía dos caminos que PIERDEN narración sin avisar. (a) `tts_engine.py:73`, filtro pensado para quitar la línea del título, se aplicaba a **todas** las líneas: cualquier párrafo que empiece por `Mi |Fui |Rechace |Hui |Compre |Regrese `, mida <200 caracteres y contenga `...` desaparecía del habla. (b) `tts_engine.py:124`, `re.sub(r'—\s*', '', text)` borraba el em dash **y el espacio siguiente**, fusionando dos palabras. Lo grave de (a) es que audio y subtítulos se generan del MISMO texto ya mutilado, así que quedan coherentes entre sí: **ninguna métrica de sincronismo, ningún auditor y ningún oído lo detectan** salvo leyendo el guion. Lo grave de (b) es que baja el conteo de palabras, que es el invariante con el que `main.py:275` fija el fin de la intro · evidencia: (a) 3 párrafos/41 palabras → 2/24; (b) 21 → 20 palabras, `"padre— era"` → `"padreera"`. No regresión: los 5 guiones reales en disco (16.896 palabras) dan sha256 **byte a byte idéntico** antes y después · arreglado: el filtro de título solo actúa en la CABECERA (`not hay_contenido`); em dash → espacio; e **invariante con dientes** al final de la función (palabras reales esperadas vs obtenidas → `raise`), calibrado sobre esos 5 corpus donde el delta medido es 0. Verificado que la guarda muerde con un sabotaje, y 8 casos degenerados (vacío, solo cabecera, solo título, em dash suelto, markdown suelto) pasan · estado: pendiente (>=2)

- [DRYRUN-01] 2026-08-14 · destruccion-irreversible-de-datos · **NO ARREGLADO — `main.py` es de otro track.** `python main.py --dry-run`, documentado en `CLAUDE.md` como *"solo genera historia sin video"*, **consume y borra el pool de gameplay**. No hay ningún guardia de `dry_run` entre el bucle de fase 2 (`main.py:456`) y `take_chunk`; los guardias existentes (`main.py:408, 469, 499, 516`) cubren ingesta, shorts, auditoría y subida, pero no el bucle. `take_chunk` hace `os.remove()` de los ficheros del pool (`gameplay_pool.py:242-245`) y `produce_video` devuelve `True` en `main.py:256` **antes** del TTS; el `finally` de `main.py:485-488` borra el chunk pase lo que pase. Resultado: horas de grabación destruidas y cero vídeos producidos. Choca de frente con la regla de oro *"NUNCA borrar `pool/`"*. La misma ruta se dispara con cualquier excepción de `produce_video` (un 429 en el bloque 3) · evidencia: lectura del código + `grep -n "dry_run" main.py` → 251, 408, 469, 499, 516, ninguno en el bucle. Riesgo hoy amortiguado: `pool/` y `test_e2e/pool/` están vacíos · estado: pendiente (>=2)

- [SHORTNUM-01] 2026-08-14 · sobrescritura-silenciosa-de-salida · **NO ARREGLADO — `main.py` es de otro track.** `main.py:117` numera los shorts con `len(existing_shorts) + 1`, mientras sus dos hermanos del mismo repo usan `max(nums) + 1` (`main.py:452` para vídeos, `dashboard_runner.py:65` para logs). Con un hueco en la numeración —un short borrado a mano, o uno que falla a mitad en `main.py:149-151`— la siguiente tanda **sobrescribe en silencio** shorts ya producidos, mp4 y `_title.txt`. `CLAUDE.md` documenta esto como *"conteo desde disco para evitar colisiones"*: la doc afirma la garantía que el código no da · evidencia: con `short_001, 003, 004` en disco, `len()+1 = 4` → regenera sobre `short_004`; el idioma correcto daría 5 · estado: pendiente (>=2)

- [SEED-02] 2026-08-14 · premisa-falsa-heredada-en-un-handoff · `SEED_D_caza_bugs.md` afirma en su línea 12 *"Los otros tres tracks están editando ficheros concretos ahora mismo"* y sobre eso construye una tabla de propiedad que prohíbe tocar `tts_engine.py`, `main.py`, `dashboard.py` y los prompts. **La premisa era falsa**: `git branch -a` → solo `fix/alineador` y `master`; ninguna rama de los otros tracks existe, ningún fichero suyo está modificado, y el track A ya había cerrado (v0.8). Lo notable es que el propio autor **ya había corregido esa frase en los SEEDs B y C** (commit `83ceaf5`, 13-ago 18:05: *"Escribí 'el track X corre AHORA MISMO en otra sesión' como un hecho, y el seed no puede saberlo… Una sesión fresca se lo creería"*) y **se dejó el D** (mtime 16:00, sin la corrección). Consecuencia medida: 4 de los 7 bugs verificados en esta sesión viven en ficheros que el SEED prohibía o no asignaba —`modules/script_generator.py` (1434 LOC) **no aparecía en ninguna fila de la tabla**, y ahí estaba [META-01]—, que es el mismo mecanismo por el que [BASURA-03] se coló 24 h antes (*"no tenía dueño en ningún track"*). Desbloqueado por decisión de Diego tras el `/seed-review` · evidencia: `git branch -a`, `git status`, `git show 83ceaf5`, mtimes de los cuatro seeds · estado: pendiente (>=2)

- [COMPET-01] 2026-08-14 · premisa-falsa-heredada-en-un-handoff (2.ª del mismo día, ver [SEED-02]) · `SEED_C_competencia.md` encuadra su hueco nº1 como *"`trend_advisor` inyecta las directrices **solo** en `reddit_story.txt`… así que el trabajo de competencia no toca a los ~50 shorts"*, o sea: el mecanismo funciona en el vídeo largo y falta replicarlo al gemelo. **Falso: `apply_to_prompt` no se ha ejecutado JAMÁS.** Ni un carácter del análisis de competencia ha entrado nunca en ningún vídeo — no es "el 98% de los artefactos se queda fuera", es el 100%. Lo destapó el agente CIEGO del `/seed-review`, que no vio el SEED · evidencia: `grep -rn "TENDENCIAS AUTO" prompts/` → exit 1 (cero marcadores); **no existe `prompts/reddit_story.txt.bak`** y `apply_to_prompt` respalda SIEMPRE; `git log --oneline -- prompts/reddit_story.txt` → 1 commit (`db338bb Estado inicial`), prompt virgen; `data/competition_advice.json` congelado el 5-ago mientras las producciones son del 10, 11, 12 y 13-ago · agravante que el SEED tampoco nombra: `_load_prompt_template` tiene **una sola llamada** (`script_generator.py:759`, primer bloque) y `_generate_continuation` monta sus mensajes desde cero, así que aun aplicándolo el consejo cubre 1 de 4 bloques (**25%**) en un vídeo de 30 min · estado: pendiente (>=2)

- [COMPET-02] 2026-08-14 · garantía-afirmada-en-doc-y-falsa-en-código (clase [DOC-01], 2.ª incidencia) · `CLAUDE.md` y `docs/video_guion.md` afirman que `remove_from_prompt` devuelve el prompt **byte a byte** al original, y el criterio de aceptación (a) del SEED exige demostrarlo. **Fallaba en los dos ficheros, antes de tocar nada**: los `open(path,"w",encoding="utf-8")` no pasaban `newline=""`, así que en Windows —la única plataforma donde corre esto— cada escritura traducía LF→CRLF · evidencia: round-trip binario sobre copias, `reddit_story.txt` **5094 → 5171 B, CRLF 0 → 77** (+1 byte por cada una de sus 77 líneas) y `short_story.txt` **1894 → 1934 B, CRLF 0 → 40**; normalizando saltos de línea sí era idéntico, o sea que una verificación en modo texto habría cantado victoria · el ejecutor del SEED solo tenía dos salidas, las dos malas: gastar el track en un bug de saltos de línea ajeno al objetivo, o "verificarlo" con una comparación que lo esconde · arreglado preservando el estilo del fichero (`_detect_newline`), no imponiendo LF: `core.autocrlf=true` y no hay `.gitattributes`, así que en un clone nuevo los prompts llegan en CRLF — verificado después con el fichero ya convertido a CRLF por un `git checkout`, round-trip idéntico igualmente · estado: pendiente (>=2)

- [COMPET-03] 2026-08-14 · borrado-silencioso-sin-red (clase de [LIMPIEZA-01], mismo día) · `strip_injection`, ante un `BEGIN_MARK` sin `END_MARK` (bloque truncado), hacía `return text[:start].rstrip() + "\n"`: **borraba todo lo posterior, incluida la línea final `Historia:`** de la que depende el prompt entero. Y `remove_from_prompt` era la única de las dos rutas que **no hacía `.bak`**, con `.bak` en `.gitignore`: irrecuperable incluso desde git. Además usaba `find` y no un bucle, así que con dos bloques inyectados quitaba el primero y devolvía `True` dejando el segundo dentro · evidencia: caso degenerado reproducido sobre copia — con el código viejo el ancla desaparecía; con el fix, `remove_from_prompt` → `False`, **0 bytes cambiados**, `Historia:` y las reglas del short intactas, y `logger.error` ruidoso · limitación declarada, NO arreglada: con dos bloques donde el segundo se pegó a mano DESPUÉS del ancla, el round-trip deja **1 byte** de más (un `\n` final). `apply_to_prompt` inserta siempre ANTES de `Historia:`, así que el código no puede producir ese estado · estado: pendiente (>=2)

- [QUOTA-01] 2026-08-14 · contabilidad-de-cuota-perdida-en-silencio · El contador de la YouTube API (10.000 unidades/día, tope duro compartido por el escaneo y el subidor) tenía tres agujeros. (a) En `scan()`, `save_state` vivía **fuera del `try`** y sin `finally`: cualquier error que no fuera `QuotaExhausted` perdía el gasto ya hecho. (b) `save_state` era `json.dump` directo, **sin lock ni atomicidad**, y `scan()` sostiene el state en memoria desde `load_state` (~L898) hasta el guardado (~L1216), minutos de clasificación LLM en medio: si el subidor guardaba en esa ventana, el escaneo lo sobrescribía. (c) Un corte a mitad de escritura dejaba el JSON ilegible y `load_state` **reiniciaba en silencio** → contador a 0 (el corte preventivo deja de proteger el resto del día, con el subidor gastando 1.650/vídeo) y los **221 canales** acumulados desde el 5-ago borrados; `data/` está gitignored · evidencia: (a) con un 404 inyectado en `channels.list` tras las 4 búsquedas → **401 unidades gastadas contra Google, 0 registradas en disco**; (b) intercalado escaneo↔subidor reproducido → se perdían **1.650** unidades y el paso 4 del protocolo del SEED ("comprueba que el delta es el que esperabas") **daba CUADRA**, porque el que pisa es el que comprueba; (c) fichero truncado → `load_state` → `{'quota': {}, 'competidores': 0}` · arreglado: `finally` (la excepción sigue propagando), `tmp+os.replace`, lock de fichero con timeout, y el contador con read-modify-write bajo el lock; el corrupto se preserva como `.corrupt-<ts>` con log ERROR · nota: `youtube_uploader.py` importa 6 símbolos de aquí y **cambia de comportamiento sin que su fichero se toque** (ahora `save_state` puede bloquear hasta 15 s) · estado: pendiente (>=2)

- [COMPET-04] 2026-08-14 · default-de-ESCRITURA-apuntando-a-producción · Al enseñar a `apply_to_prompt` a operar sobre los dos prompts, `_path_for(config,"short")` copió el idiom de lectura de `shorts_generator.py:107` — `config["paths"].get("short_prompt", "./prompts/short_story.txt")` — pero aquí ese valor es un destino de **escritura**. Consecuencia: cualquier llamante que no declare `short_prompt` (un test, el config del gate, un script de medición) reescribe el prompt de PRODUCCIÓN sin haberlo nombrado nunca · evidencia: ocurrió de verdad durante la verificación — un script de casos degenerados que apuntaba `prompt_template` a una copia dejó **2 bloques inyectados y un `.bak`** en `prompts/short_story.txt` real (2882 B, `grep -c "TENDENCIAS AUTO"` → 2). Solo se detectó porque ese script comprobaba explícitamente que el fichero real siguiera intacto (`CASO 6`); ningún otro test lo miraba. Recuperado con `git checkout` · arreglado: para escribir **no hay ruta por defecto** — o el config la declara, o se omite con log ruidoso (implícito) o `raise` (si se pidió ese perfil a propósito) · estado: pendiente (>=2)

- [INSTR-07] 2026-08-14 · instrumento-que-da-veredicto-sobre-el-conjunto-vacío (clase de la regla madre de §D) · El script del A/B que decide si la inyección merece existir falló sus 20 generaciones —importa los módulos directos y nunca llamó a `load_dotenv()`, que `main.py:395` sí hace— y aun así imprimió su cabecera de `RESULTADO` con `n=0` en los dos brazos y **salió con código 0**. Un instrumento que emite veredicto sobre el conjunto vacío es peor que no medir: el veredicto tenía el mismo aspecto que uno bueno · evidencia: `RuntimeError: Falta la API key de OpenRouter` ×20, `"brazo": "A_control", "n": 0`, `[exited with code 0]`; 0 peticiones gastadas · arreglado en el propio instrumento: mínimo de muestra por brazo y salida != 0 antes de calcular nada · estado: pendiente (>=2)

- [TRUNCA-01] 2026-08-14 · vídeo-publicable-que-no-cuenta-lo-que-promete · `video_007` (gate del track C) anuncia en su TÍTULO —y por tanto en la miniatura y en la intro— *"…Pero El Notario Descubrió La Coacción Y Anuló Todo"*, y **ese desenlace no se narra jamás**. El modelo entregó 2300 palabras para un objetivo de 623 y `_truncate_to_words` descartó **1688 palabras del CUERPO (frases 9-46 de 51)**, conservando las últimas 238 como epílogo. La costura cae en t=1:57,3 → 1:58,2 de un vídeo de 3:10, y los 72 s finales (38%) son epílogo de escenas que el espectador no ha visto (*"pienso en la jueza cansada y en el notario anciano que murió el invierno pasado"* — personajes descartados) · evidencia: `pipeline.log` 19:37:51 *"se descartan 1688 palabras del CUERPO … Esto introduce un SALTO en la narracion"*; sobre `video_007_story.txt`, `anul-` aparece **solo en la posición 133, que es el título** (0 veces en el cuerpo narrado), y `juez`/`juzgado` solo en 2331/2308 (epílogo) · **el gate lo dio por bueno**: `audit_run.py:228 cierre_narrativo()` solo comprueba que el bloque de desenlace SE PIDIÓ, y `grep -n "truncad\|SALTO\|descartan" scripts/audit_run.py` → **0 resultados**: el warning más ruidoso del log no lo lee ningún check · es la 2.ª incidencia del "salto del truncado" (la 1.ª, 1645 palabras, en sessions-log v0.6) · **NO ARREGLADO: `main.py`/`script_generator.py` no son del track C, y el track D ya había cerrado cuando se detectó — queda sin dueño** · estado: pendiente (>=2)

- [SHORTVID-01] 2026-08-14 · anti-repetición-con-un-lado-descubierto · `short_010` cuenta **el mismo argumento que el vídeo largo de su propia corrida**: `video_007` = *"Mi Hermana Manipuló A Mi Madre Enferma Para Cambiar El Testamento… Pero El Notario Descubrió La Coacción"*; `short_010` = *"Mi Cuñada Cambió Mi Testamento Mientras Yo Estuve En Coma Y El Notario La Delató"*. Mismo mecanismo: familiar altera un testamento aprovechando la incapacidad de la víctima → el notario lo detecta → se anula · causa estructural en `main.py:124-134`: `titulos_previos` se siembra con `glob("short_*_title.txt")` y **nunca incluye el título del vídeo padre**, así que `generate_short(avoid=...)` no puede evitarlo. El fix de los "4 shorts iguales" cubrió short↔short y dejó short↔vídeo sin cubrir · evidencia: solape léxico de contenido contra el título del largo — `short_010` da 2/9 (`notario`, `testamento`) frente a 0-1/8-12 de los otros 11 shorts en disco; los cuerpos confirman la misma trama · atenuante medido: los 8 shorts **entre sí** sí son argumentos distintos y 8/8 títulos únicos — no es la reincidencia de "4 shorts con la misma historia" · **NO ARREGLADO (main.py, sin dueño)** · estado: pendiente (>=2)

- [APERTURA-01] 2026-08-14 · garantía-en-prosa (5.º episodio de §17) · El guardia de apertura de shorts **se disparó en 4 de 4 shorts de la corrida y el modelo lo ignoró en 4 de 4**. `shorts_generator.py:128-137` inyecta en el prompt *"APERTURA OBLIGATORIAMENTE DISTINTA: … El tuyo NO puede empezar por esa palabra"* y **no hay ningún `if` que rechace y reintente** el título que la viola — igual que las comas (167/129/0/0) y el título forzado · evidencia: reproducción offline determinista con el estado real de disco previo a `short_009` → `guardia -> 'mi'`, `INYECTADA: True`, `CUMPLE: False` en 009, 010, 011 y 012; agregado sobre los 12 títulos en `shorts/`: **12 de 12 empiezan por "Mi "**, 11 de 12 usan " Y " como conector del desenlace · agravante en el instrumento: `audit_run.py:673` marca `OK` si `maxracha <= 6`, y acotado a los 4 shorts de la corrida la salida es `OK aperturas: racha máxima 4` — el defecto sale en VERDE, y aunque saltara sería `AVISO`, nunca entra en `fallos` · **NO ARREGLADO (shorts_generator.py, sin dueño)** · estado: pendiente (>=2)

- [GATE-05b] 2026-08-14 · residuo-de-un-fix-a-medias · Tras el fix de [GATE-05] (`ef3f963`, que enseñó al auditor a medir la corrida y no el directorio), el gate del track C **midió el sincronismo de 2 shorts de OTRA corrida y de NINGUNO de los 4 suyos**: `OK short_005 … OK short_006 … AVISO cobertura: 2/8 medidos -- SIN medir (6): short_007…short_012`. La capacidad existe pero `main.py:328` invoca el auditor con `--shorts` y **sin `--shorts-stems`**; el propio `audit_run.py:824` lo admite (*"main.py todavia NO pasa este flag"*). Consecuencia: "8/8 títulos únicos" y la racha de aperturas se calcularon sobre la mezcla de dos corridas · evidencia: pasando los stems a mano, los 4 shorts reales dan 0,023-0,032 s de error medio y 0,00 s de voz sin subtítulo — **no había defecto de sincronismo; el problema es que el gate no lo sabía** · clase: la capacidad se añade en la herramienta y el llamante no la usa · estado: pendiente (>=2)

- [PARALELO-01] 2026-08-14 · dos-sesiones-un-solo-árbol-de-trabajo · Los tracks C y D corrieron a la vez sobre el mismo directorio. Git es por repositorio, no por sesión: al crear yo `feat/competencia`, el track D —que se creía en otra rama— **commiteó encima de la mía** sin saberlo. Nada se perdió (los commits son aditivos y no hubo solape de ficheros: C tocó `trend_advisor.py`/`competitor_scout.py`, D `script_generator.py`/`shorts_generator.py`/`tts_engine.py`/`audit_run.py`), pero un `git checkout` de cualquiera de las dos sesiones se habría llevado por delante el trabajo sin commitear de la otra · evidencia: `git log` → `935e269` (C, 11:15) sobre `f13a5ea` (D, 11:10) sobre `684ed31` (C, 11:03); `git branch -a` → solo `feat/competencia`, `fix/alineador`, `master` · riesgo colateral medido: el A/B de C importó `shorts_generator.py` 18 s después de que D lo modificara (11:13:05 vs 11:13:23); no invalidó la medición porque Python importa una vez y los DOS brazos corrieron en el mismo proceso con el mismo código, pero eso fue suerte de calendario, no diseño · estado: pendiente (>=2)

- [GATE-04] 2026-08-14 · el-gate-falla-ABIERTO (la nota perfecta de no medir nada) · `scripts/audit_run.py`, que es lo único entre un defecto y YouTube, daba verde por tres caminos distintos sin haber medido nada. (a) **Conjunto vacío**: con 0 vídeos y 0 shorts el bucle no itera, `fallos` queda vacío y el script imprime `VEREDICTO: sin defectos MEDIBLES` con **EXIT=0** — y `main.py:346` interpreta `returncode == 0` como "la salida está sana". Lo mismo con un `--stem` que no casa con ningún vídeo (el `continue` no deja rastro). (b) **Sin `try/except` por vídeo**: `audita_video` (:661) corría desnudo y `audita_shorts` (:665) estaba FUERA del bucle, así que una excepción en un vídeo se llevaba por delante los vídeos posteriores **y la auditoría de shorts entera**. (c) **Instrumentos que fallan abierto**: `_ffprobe` no miraba `returncode` y devolvía `stdout.split()`; `_silencios` devolvía `[]` con ffmpeg caído, y `exceso = max(0, 0 - signos) = 0` daba **"0,0 pausas por 1000 palabras" = la nota perfecta**, indistinguible de un audio limpio. `loudness` tenía la misma forma. La métrica está viva y cerca del umbral (`video_001` 14,25 falla / `video_002` 9,56 pasa, umbral 12,0), así que decide de verdad · evidencia: conjunto vacío ANTES `VEREDICTO: sin defectos MEDIBLES` + `EXIT=0`, DESPUÉS `FALLA no se ha auditado NADA... un conjunto vacío no es una salida sana, es una salida sin medir` + `EXIT=1`; `_silencios`/`_ffprobe` sobre fichero inexistente ANTES `[]`/`[]` → `exceso 0, por_mil 0.0`, DESPUÉS `None`/`None` con aviso; excepción por vídeo ANTES traceback sin capturar y 0 `_audit.json` escritos, DESPUÉS cada vídeo recibe el suyo y los shorts se auditan igual · arreglado: `None` = "no se pudo medir" propagado a los 4 instrumentos y a sus 5 consumidores (nunca se trata como 0), `try/except` por vídeo + `audita_shorts` siempre, y fallo explícito si no se auditó nada · **es la 3.ª incidencia de la clase [GATE-02] "un guardia nuevo que da cero en verde es sospechoso"**, y esta vez el ciego lo dijo así: *lo que hay que proteger no son los vídeos, es la distinguibilidad entre "medido y correcto" y "no medido"* · estado: pendiente (>=2)

- [WOOSH-01] 2026-08-14 · constante-documentada-que-el-codigo-no-implementa · `settle_time` ("tiempo hasta que la tarjeta de título llega al centro") está **asignado y nunca usado** en los DOS gemelos (`video_composer.py:92`, `shorts_generator.py:332`; pyflakes lo marca en ambos). La animación real es `x(t) = W*exp(-12t)*cos(18t)`, que cruza el centro cuando `cos(18t) = 0` → **t = π/36 = 0,087 s**, no 0,25. Y como `woosh_offset = max(0, settle_time - WOOSH_PEAK) = max(0, 0.25 - 0.483) = 0`, el pico del woosh suena en **0,483 s**, es decir ~0,40 s DESPUÉS de que la tarjeta ya ha aterrizado. `CLAUDE.md` afirma "woosh sound sincronizado con llegada al centro (pico a 0.25s)": ninguna de las dos cifras corresponde al código. Sincronizarlo de verdad exige **recortar la cabeza del mp3**, no retrasarlo (el offset necesario es negativo) · **NO ARREGLADO a propósito**: cambia el *feel* de la intro de todos los vídeos y eso es juicio de Diego (capa 3), no una medición · evidencia: `grep -n settle_time` en los dos ficheros + pyflakes + la aritmética del primer cruce por cero · estado: pendiente (>=2)

- [TRUNC-01] 2026-08-14 · el-video-promete-un-suceso-que-nunca-narra · Destapado por `output-audit` en el `/eval` de cierre del track D (`video_006`, fixture de 3,2 min) y confirmado por ejecución. El modelo sobregeneró (bloque 1 = **1392 palabras para un objetivo de 623**), el cierre añadió 1254 más, y `_truncate_to_words` (`script_generator.py:1121`) **descartó 2032 palabras del cuerpo — las frases 9-48 de 52, el 77%**. Resultado medido: las tres entidades que el TÍTULO promete (`abogado`, `falsificación`, `desheredó`) aparecen **0 veces fuera de la frase del título**, y esa frase es también la MINIATURA y la tarjeta de INTRO. La narración salta de "cuidaba a mi madre" al epílogo y presenta a *"Don Rafael"* como personaje conocido sin haberlo introducido nunca. **El gate lo dio en verde**: `audit_run` comprueba que el bloque de desenlace *existe* (`OK cierre narrativo: el bloque de desenlace se pidio y se escribio`), no que la narración sea continua, y escribió `video_006_audit.json` con `ok:true` — el fichero que el dashboard usa para ofrecer la subida · evidencia: `pipeline.log` `17:54:20 [WARNING] Truncado: se descartan 2032 palabras del CUERPO (frases 9-48 de 52)`; conteo de entidades sobre `test_e2e/temp/video_006_story.txt` · **NO es regresión del track D**: el truncado es preexistente y ya estaba señalado como abierto en `sessions-log` v0.6 (*"el salto de 1645 palabras que introduce el truncado"*), ahora medido en 2032 y con la consecuencia narrativa probada · **NO ARREGLADO** · estado: pendiente (>=2)

- [TITULO-03] 2026-08-14 · gemelo-no-propagado (`_ensure_title_at_start` mutila la 1.ª frase, ahora en shorts) · En el mismo `/eval`, **2 de los 4 shorts nuevos** (`short_006`, `short_008`) narran y subtitulan como primera frase del cuerpo un **fragmento subordinado sin sujeto ni verbo principal**, empezando en minúscula justo después del punto del título: `"...Le Obligó A Reconstruirlo Entero. una madrugada para ampliar su piscina sin pedir permiso..."` y `"...Y El Comprador Lo Rastreó. mientras yo estaba en urgencias tras un ataque severo..."`. Confirmado en el AUDIO real y en el `.ass` (primer cue `UNA` en t=4,23 s; `MIENTRAS` en t=3,55 s). Causa: `_ensure_title_at_start` (`script_generator.py:733-736`) borra el solapamiento literal del título (`17:58:30 Eliminado solapamiento de 5 palabras del inicio`) **sin comprobar que lo que queda sea una frase completa**, y el guardia de eco de `:750` no lo caza porque no son ecos puros (`_es_eco_del_titulo` → False en los dos, medido). **Es la misma clase que se cerró para el vídeo largo en v0.7 y no se propagó al gemelo vertical** (§11). En un short de 35-40 s, el segundo 3 ES el producto. El auditor lo dio en verde (`OK arranque de shorts: 0 narran un trozo repetido de su propio título`): una regla determinista de una línea —"la frase siguiente al título empieza en minúscula"— habría cazado 2 de 4 · **NO es regresión del track D** (el único cambio en esa función fue borrar un `import re as _re` sin usar) · **NO ARREGLADO** · estado: pendiente (>=2)

- [SHORTDUR-01] 2026-08-14 · spec-caducada-o-constante-a-la-mitad, sin guardia · Los 4 shorts de la corrida duran **34,2 / 35,5 / 40,6 / 41,2 s**, contra los **60-90 s** que declaran `CLAUDE.md` y las reglas. No es del fixture: `config.yaml` de PRODUCCIÓN tiene los mismos `target_words: 200`, `narration_wpm: 200`, `speed: 1.5` → 200/200 = 60 s a 1x = **40 s a x1.5**, y el propio log lo dice (`17:55:56 Shorts: 191s gameplay / 40s por short`). O la spec está caducada o `target_words` está a la mitad; **ningún guardia comprueba la duración final del short**, así que nadie lo mide · evidencia: `ffprobe` de los 4 mp4 + los dos `config.yaml` · **NO ARREGLADO** (decidir cuál de las dos cifras es la correcta es de Diego) · estado: pendiente (>=2)

- [GATE-05] 2026-08-14 · el-gate-mide-el-DIRECTORIO, no la corrida · En el `/eval` de cierre, `audit_run` reportó `artefactos: 8 mp4 y 12 títulos` y cantó `VEREDICTO: NO PUBLICABLE — faltan artefactos de shorts`. **Es un falso positivo**: los "títulos sin mp4" son `short_001..004_title.txt` huérfanos de la corrida anterior. Y mientras canta ese fallo inexistente, **NO canta los dos reales** ([TRUNC-01] y [TITULO-03]). La misma contaminación afecta a `títulos únicos: 12/12` y a `aperturas: racha máxima 11 títulos seguidos`, que mezclan dos corridas. Además el veredicto global (`NO PUBLICABLE`) y el fichero que consume el dashboard (`video_006_audit.json` con `ok:true`) **se contradicen**: los fallos de shorts nunca entran en el JSON por vídeo (`audit_run.py:786`). Añadido: con `--shorts 2` solo se midió el sincronismo de 2 de los 4 shorts · evidencia: mtimes de `test_e2e/shorts/` (títulos 001-004 de las 09:51-10:13, mp4 nuevos de las 17:58-18:05) + el veredicto del log + el JSON · **NO ARREGLADO** · estado: pendiente (>=2)

- [GATE-05-fix] 2026-08-14 · cierre de [GATE-05] · `audita_shorts` empareja ahora por **stem**: un PAR completo es `<stem>.mp4` + `<stem>_title.txt`, y solo los pares entran en variedad, arranque y sincronismo. Un `_title.txt` sin su mp4 es un huérfano de otra corrida → **AVISO nombrándolo**, nunca `fallos`; un `.mp4` sin su título **sigue bloqueando** (un short compuesto sin nada que publicar). Añadido `--shorts-stems` para acotar TODA la auditoría de shorts a la corrida, y un aviso explícito de **cobertura de sincronismo** (cuántos de cuántos shorts se midieron y cuáles no), porque con `--shorts 2` el informe callaba que los demás quedaban sin medir · evidencia medida sobre el directorio REAL `test_e2e/shorts` (4 títulos huérfanos + 8 pares): ANTES `OK artefactos: 8 mp4 y 12 títulos` → `VEREDICTO: NO PUBLICABLE — faltan artefactos de shorts`, `EXIT=1`, con variedad calculada sobre 12 títulos de dos corridas (racha 11); DESPUÉS `OK artefactos: 8 par(es) completos` + `AVISO títulos huérfanos ... short_001, short_002, short_003, short_004`, `EXIT=0`, variedad sobre 8 pares (racha 7); con `--shorts-stems short_005..008`, 4 pares y racha 4. Casos degenerados: directorio vacío → bloquea, solo huérfanos → bloquea, stem pedido inexistente → bloquea, mp4 sin título → bloquea. El guardia de conjunto vacío de [GATE-04] sigue vivo · **queda pendiente 1 línea en `main.py`** (fichero de otro track) para pasar `--shorts-stems` con los stems recién generados; sin ella el default ya es correcto, pero la medida de variedad sigue viendo el directorio entero · estado: cierra [GATE-05]

- [TITULO-03-fix] 2026-08-14 · cierre de [TITULO-03] · `_descartar_fragmento_inicial` en `script_generator.py`: tras borrar el solapamiento literal del título, si lo que queda **no empieza frase** (primera letra en minúscula) es la cola de la frase que el modelo escribió, y se descarta entera. Señal determinista que no depende del modelo (§18): en español, detrás de un punto va mayúscula. Se abstiene si no hay una frase completa detrás o si borrarla vaciaría el speech · **calibración que importa**: una primera versión saltaba también la puntuación de apertura (`¿ ¡ « " ( —`) y con eso **se comía una pregunta retórica y un diálogo enteros** (`«callate», dijo. Y se fue.` → `Y se fue.`); se hizo conservadora —solo espacios— porque borrar narración legítima es el lado caro (§16) · evidencia A/B sobre los 4 shorts REALES de la corrida: `short_005` `'Años'` y `short_007` `'Firmé'` **idénticos**; `short_006` `'una'` → `'Llegué'` (−11 palabras), `short_008` `'mientras'` → `'Publicó'` (−24). Vídeo largo: sha256 **idéntico** (`48d094d6aaaf555d`, 614 palabras). 9 casos degenerados, solo dispara el real · estado: cierra [TITULO-03]

- [DOC-02] 2026-08-14 · la-lista-de-NO-BORRAR-protegia-un-path-inexistente · La lista de "nunca borrar" nombra `test_e2e/clip.mp4` en **4 sitios** (`CLAUDE.md:49`, `.claude/skills/eval/SKILL.md` x2, `.claude/agents/engineer.md:84`) y ese fichero **NO EXISTE**: el fixture real vive en `test_e2e/input/clip.mp4` (1,5 GB, 05-ago), porque `test_e2e/config.yaml` declara `input_dir: ./test_e2e/input` · salió al limpiar disco antes de una corrida larga: un `ls test_e2e/clip.mp4` de verificación devolvió `No such file or directory` y durante unos segundos pareció que la limpieza se había llevado el fixture del gate. No fue así (los borrados eran explícitos y ninguno lo tocaba), pero **una limpieza que se fiara de la lista habría creído estar protegiéndolo mientras borraba `test_e2e/input/`** · es la clase T1 / [DOC-01]: un dato escrito en un `.md` se propaga como verdad y caduca en silencio — aquí lleva vivo desde que se escribió el fixture y ninguna de las ~10 sesiones que leyeron la lista lo comprobó, porque nadie ejecuta una lista de prohibiciones · corregidos los 3 ficheros no inmutables; **`CLAUDE.md:49` pertenece al conjunto INMUTABLE** (`produccion-loop.md` §E: solo Diego lo toca) y queda señalado, no editado · lección operativa barata: una ruta escrita en una regla se verifica con `ls` la primera vez que se lee, no la primera vez que se necesita · estado: pendiente (>=2)

- [DRYRUN-01-fix] 2026-08-14 · cierre de [DRYRUN-01] · `take_chunk(config, dry_run=False)`: con el flag calcula la MISMA duración sin tocar el disco —no parte ficheros, no mueve el sobrante, y sale ANTES de los `os.remove` del pool— y devuelve `DRY_RUN_CHUNK`, una ruta que **no existe a propósito** para que el `os.remove` del `finally` de `main.py` no encuentre nada. En `main.py`, además del flag, un `break` **obligatorio**: sin consumir el pool, `take_chunk` devolvería el mismo chunk en cada vuelta y el `while True` no terminaría nunca (lo verificó el repro E2E, no la lectura) · evidencia A/B sobre un pool de juguete de 5 ficheros: **HEAD 5 → 0 ficheros (POOL DESTRUIDO)**, con el fix **5 → 5 (POOL INTACTO)**, misma duración calculada (203 s) y la ruta devuelta no existe en disco. No regresión del camino de producción (`dry_run=False`): 203 s, pool consumido y chunk creado, **idéntico en las dos versiones**. E2E real con el comando documentado (`python main.py --config <toy> --dry-run --skip-ingest`, 2 peticiones): `EXIT=0`, `dry-run: se usarian 203s ... El pool NO se toca`, `Modo dry-run: una sola historia, el pool queda intacto`, `Pipeline finalizado: 1 videos producidos`, y **5 ficheros en el pool antes y después** · estado: cierra [DRYRUN-01]

- [SHORTNUM-01-fix] 2026-08-14 · cierre de [SHORTNUM-01] · `main.py` numera los shorts por el **máximo** (`max(nums)+1`) en vez de por el conteo, que es el idioma que ya usaban sus dos hermanos (`main.py` para vídeos, `dashboard_runner.py` para logs) · evidencia, incluido el caso REAL que se dio hoy en `test_e2e/shorts` (4 mp4 numerados 009-012 tras perderse los anteriores): `len+1` daba **5** → habría sobrescrito `short_009..012`; `max+1` da **13**. Con un hueco (`001, 003, 004`): `len+1`=4 (machaca `short_004`) → `max+1`=5. Sin hueco (`001, 002`): **3 y 3, idéntico**; directorio vacío: **1 y 1** · limpieza aparte en los dos ficheros tocados: fuera el `target_min` muerto de `main.py`, el import `get_video_duration` sin usar, y el bloque de `gameplay_pool` que calculaba `remaining_pool` y terminaba en `pass` (no hacía nada). `pyflakes` limpio en `main.py` y `gameplay_pool.py` · estado: cierra [SHORTNUM-01]

- [APERTURA-02] 2026-08-14 · CORRECCIÓN de [APERTURA-01] + guardia-que-se-anula-a-sí-mismo · **Mi entrada [APERTURA-01] atribuía mal la causa.** Decía *"no hay ningún `if` que rechace y reintente"*: es **falso**, el `if` existe (`shorts_generator.py:183`) y funciona — aislado, con el modelo devolviendo siempre "Mi…", reintenta 4 veces y acepta a la 5.ª por diseño. La causa real la localizó un `bug-hunter` sin contexto y la validé por ejecución: `_apertura_agotada` (`:113`) exige **unanimidad exacta de las 5 últimas** (`len(set(ultimas)) == 1`), así que **basta UN título con otra apertura para apagarlo** — y ese título lo produce **el propio guardia cuando acierta**. Al quedar en disco con el número más alto, siembra la lista de la tanda siguiente y desactiva el guardia durante **exactamente 4 shorts, el tamaño de una tanda**: su éxito es lo que lo apaga · evidencia: semilla real `mi mi mi mi mi mi mi descubrí` (el `descubrí` es `pipeline.log:953`, *"Descubrí Que Mi Tío Falsificó Mi Adopción…"*, generado por el guardia a las 10:54) → `racha=None` en los 4 shorts, cero warnings, coincidiendo bit a bit con el log de la corrida de las 19:39; y corroboración **predictiva**: el único short de todo el log donde el predicado se activa es justo el que salió "Descubrí Que…" · **lección de método**: mi inferencia *"no hay warnings ⇒ el guardia no existe"* era falsa porque el canal principal del guardia es la **inyección en el prompt, que es muda**; en 3 disparos medidos el modelo obedeció a la primera sin dejar rastro. Un guardia sin `logger.info` al detectar es indistinguible de un guardia ausente · estado: pendiente (>=2)

- [APERTURA-03] 2026-08-14 · gemelo-AUSENTE: el vídeo largo no tiene guardia de apertura · Al buscar el gemelo de [APERTURA-02] resulta que el gemelo no está roto: **no existe**. `generate_story(target_words, style, config)` no recibe `avoid` ni conoce las aperturas ya usadas, así que no hay ningún mecanismo —ni roto ni sano— que impida que todos los títulos de vídeo empiecen igual · evidencia: **9 de 9** `*_title.txt` de `output/` y `test_e2e/output/` empiezan por "Mi"/"Mis" · matiza §11: la tabla de pares gemelos busca "el fix no propagado", y aquí el hallazgo es "la defensa nunca se escribió en el otro lado" — el gemelo desatendido es el vídeo LARGO, no el short, que es el orden inverso al habitual en este repo · agravante: `scripts/audit_run.py:677` da `OK` con `maxracha <= 6` y solo `AVISO` por encima; en la corrida de las 19:48 imprimió `AVISO aperturas: racha máxima 8` y el pipeline siguió. Su umbral (6) tampoco cuadra con el del generador (5) · **NO ARREGLADO** · estado: pendiente (>=2)

- [AUDIT-02] 2026-08-14 · el-fix-de-hoy-traia-sus-propios-defectos (verificado por panel adversarial) · Dos `bug-hunter` autocontenidos, lanzados a REFUTAR los fixes del track D, tumbaron varias de sus afirmaciones. Confirmados por ejecución propia: (a) **el invariante de `_clean_speech_for_tts` abortaba en falso**: comparaba con `!=`, así que saltaba también cuando el texto GANA una palabra —y el cambio del em dash a espacio, del mismo commit, es justo lo que parte un token (`padre—era` → 2 palabras)—, con el mensaje diciendo "perdió narración" sobre un texto que no perdió nada; coste real: `main.py:473` llama a `take_chunk` ANTES del `try`, así que abortar ahí **se lleva el chunk de gameplay** además de las peticiones ya gastadas; (b) **el filtro de cabeceras estaba sin anclar** (`^(BLOQUE|PARTE|...)\s*\d*` con `\s*\d*` casando con NADA): borraba el párrafo entero de cualquier línea que EMPEZARA por esas letras —*"Bloqueé su número aquella misma noche…"*, *"Parte de mí quería perdonarle…"*, vocabulario de manual en este género— **y el invariante nuevo era ciego por construcción**, porque esas palabras se suman a `palabras_saltadas`. Es la misma clase que ese commit arregló en la línea de al lado (el filtro de títulos que se comía párrafos); (c) **la línea de cobertura de sincronismo, escrita para que "no medido" no se leyera como "sano", decía exactamente eso**: contaba lo PEDIDO y no lo medido, así que imprimía `OK cobertura: 2/2 shorts medidos (todos)` dos líneas debajo de dos avisos `NO se ha medido su sincronismo`, con `EXIT=0`; (d) el chequeo de arranque de shorts imprimía `OK ... 0 narran un trozo repetido` sin haber medido nada — el mismo short defectuoso pasaba de `FALLA` a `OK` solo con que faltara su `.srt`; (e) `--shorts-stems ""` —que es lo que produce `",".join(stems)` en una corrida sin shorts— desactivaba el acotamiento **en silencio**; (f) `peor_tramo` puede ser `None` con <40 pares y `audita_video:541` lo indexaba sin guardia, **mientras su gemelo de shorts SÍ la tenía** (§11 al revés) · evidencia: repros pegados en la sesión, todos re-ejecutados por el orquestador · arreglado (a)-(f) en `tts_engine.py` y `scripts/audit_run.py`, con no regresión sobre 20 guiones reales (delta 0, 0 abortos) y sobre el vídeo real (14 mediciones idénticas) · **lo que esto enseña: verificar los fixes rinde más que cazar bugs nuevos — 6 de los 6 defectos los introdujo el arreglo del día, no el código viejo** · estado: pendiente (>=2)

- [META-02] 2026-08-14 · el-tope-del-guardia-es-mayor-que-lo-que-protege · **NO ARREGLADO — `modules/script_generator.py` lo tiene tomado otra sesión (120 líneas sin commitear).** El backstop `_META_CORTE_MAX_FRAC = 0.15` que se añadió hoy para impedir que la limpieza de metadatos borrara el desenlace **tolera borrar 840 palabras en un vídeo de 30 min** (objetivo ~5600), cuando `_CIERRE_PALABRAS = 500` y `_CIERRE_MIN_PALABRAS = 120`: el umbral permite borrar **1,7 veces el bloque de cierre completo**, que es exactamente lo que el mensaje del `raise` dice querer impedir. Un auditor lo reprodujo con el marcador real al inicio del bloque de cierre: 621 palabras borradas = 11,7% → no aborta → guion publicado sin final. Añadido: `_limpiar_bloque` (la ruta que ahora se ejecuta SIEMPRE) **no tiene ningún tope de porcentaje**, así que puede borrar hasta un bloque entero en silencio, y su `logger.warning` imprime dentro las palabras borradas — el mismo anti-patrón que su propio docstring critica · evidencia: aritmética de las constantes (`623→93`, `5600→840` palabras toleradas) verificada por ejecución + repro del auditor con guion real · **fix propuesto: tope ABSOLUTO del tamaño de una anotación (p.ej. `max(60 palabras, 2%)`), nunca una fracción del total; y llevar el tope DENTRO de `_limpiar_bloque`** · estado: pendiente (>=2)

- [FRAG-01] 2026-08-14 · guardia-que-borra-narracion-legitima · **NO ARREGLADO — mismo fichero tomado.** `_descartar_fragmento_inicial` (el fix de [TITULO-03]) asume que lo que borra "son justo las palabras que el título ya dice", pero el solapamiento es un match por PREFIJO que rompe en la primera palabra distinta: en cuanto la frase del modelo diverge del título, lo que borra es narración nueva. Medido por un auditor en el camino real del short: **41% del cuerpo borrado (163 → 96 palabras)**, por encima del mínimo de 80 así que el short se publica igual. `_FRAGMENTO_MAX_CHARS = 400` ≈ 66 palabras, contra los casos reales medidos de 11 y 24: el techo está a 2,7x del máximo observado. Además NO dispara en tres formas realistas: fragmento que arranca con cifra (`isalpha()` es False), corte en abreviatura (`Sr.`, `Dr.`, `etc.`), y dos frases descabezadas seguidas (una sola pasada) · **y la evidencia de no regresión que se dio al cerrar [TITULO-03] era débil**: de los 18 pares (título, guion) en disco, **0 llegan a la rama nueva** —todos salen por el `return` temprano— así que el A/B no podía ver un falso positivo por construcción · **fix propuesto: tope de palabras del fragmento (~28) + exigir solape real con el título, o mejor, DESHACER el recorte del prefijo en vez de borrar la cola** · estado: pendiente (>=2)

- [TRUNCA-02] 2026-08-14 · cortar-por-el-MEDIO-se-lleva-el-climax-aunque-la-fraccion-sea-pequeña · Arreglado el sobrepaso ORDENADO por el código ([TRUNCA-01]: el prompt pedía `MÍNIMO 2000` palabras ignorando `target_words`, y `num_blocks = max(2,…)` ponía un suelo de 2 bloques → 84% de sobrepaso ordenado en el fixture), el descarte del fixture baja de **73% a 21%** … **y el vídeo sigue prometiendo en el título algo que no narra**. `video_008`: *"…Pero El Juez **Anuló** La Escritura Cuando **Presenté** Los **Registros** Médicos Que **Demostraban** Incapacidad"* → en el cuerpo narrado `anul`=0, `demostr`=0, `present`=0, `registr`=0 · causa: `_truncate_to_words` corta por el MEDIO y conserva las últimas N palabras como "desenlace"; el modelo escribió setup → resolución (frases 17-25 de 35) → epílogo, y el truncado tiró justo la resolución y conservó un epílogo genérico (*"Aprendí que el amor no se mide en herencias…"*). **La fracción descartada no es el predictor**: 21% bastó · lo bueno: el check de coherencia título↔cuerpo añadido con [TRUNCA-01] **lo cazó**, así que el gate pasó de aprobar un vídeo roto a rechazarlo correctamente (`VEREDICTO: NO PUBLICABLE`) · **NO ARREGLADO**: el arreglo de fondo es no sobrepasar (pedir menos y aceptar la historia tal cual, o regenerar) en vez de recortar por el medio; el objetivo de palabras es una restricción REAL porque el vídeo no puede durar más que su gameplay · estado: pendiente (>=2)

- [GATE-06] 2026-08-14 · check-acoplado-a-una-linea-de-log-que-el-fix-dejo-de-emitir · `scripts/audit_run.py` → `cierre_narrativo()` decide si la historia tiene desenlace **buscando en `pipeline.log` que se pidiera el bloque de cierre**. El camino nuevo de un solo bloque (historias que caben en `WORDS_PER_BLOCK`) escribe el desenlace **dentro** de la misma llamada y no emite esa línea, así que el auditor marcó `FALLA cierre narrativo: el bucle salio por el 85% del objetivo` sobre un `video_008` que **sí tiene final** (verificado: termina en *"Ahora duermo tranquilo sabiendo que hice lo correcto…"*) · es un **falso positivo introducido por mi propio cambio**, y de la misma clase que [GATE-05b]: la capacidad cambia en un sitio y el check que la observa se queda anclado a la señal vieja · agravante: un falso positivo en un gate con dientes es caro, porque entrena a ignorarlo · **NO ARREGLADO** · estado: pendiente (>=2)

- [META-02-fix] 2026-08-14 · cierre de [META-02] · Dos cambios. (a) **Tope DENTRO de `_limpiar_bloque`**, que no tenía ninguno: el defecto no se había cerrado, se había movido de "corta hasta el final de la concatenación" a "corta hasta el final del BLOQUE". Va por **fracción del bloque** (`_META_CORTE_MAX_FRAC_BLOQUE = 0.35`) porque lo que distingue metadato de narración es la POSICIÓN del marcador: el corte llega hasta el final, así que uno en la cola se lleva poco y uno enterrado se lleva casi todo. Calibrado contra el caso REAL de [BASURA-03] (`video_004`: 132/604 = 22%), no contra un número inventado. (b) **El backstop de la concatenación se ata a lo que protege**: antes `> 15% del total`, que en un vídeo de 30 min son **840 palabras** contra un desenlace de 500 (mínimo 120) — permitía borrar 1,7 veces el final que decía proteger. Ahora aborta si el corte se lleva `>= _CIERRE_MIN_PALABRAS` (120): a esas alturas los bloques ya se limpiaron uno a uno, así que lo que aparezca es una forma nueva y toca ser conservador. Además el `logger.warning` ya no imprime dentro las palabras borradas (llegó a meter 826 palabras de narración en una línea de log) · evidencia: metadato en la cola → limpia, 27/1421 = 2%; enterrado a mitad → **ABORTA** (751/1421 = 53%); caso real de [BASURA-03] → limpia, 176/625 = 28%, por debajo del 35% · estado: cierra [META-02]

- [FRAG-01-fix] 2026-08-14 · cierre de [FRAG-01] · `_FRAGMENTO_MAX_PALABRAS = 28`: el solapamiento que se quita antes es un match por PREFIJO que rompe en la primera palabra distinta, así que en cuanto la frase del modelo diverge del título lo que queda detrás ya no son "las palabras que el título dice", es narración nueva. Los fragmentos reales medidos son de 11 y 24 palabras; el techo anterior (`_FRAGMENTO_MAX_CHARS = 400` ≈ 66) estaba a 2,7x. Además ahora dispara con **arranque en cifra** (`isalpha()` es False para un dígito, así que *"400 euros al mes sin decírmelo…"* —descabezado igual— no se cazaba) · evidencia: los dos casos reales (11 y 24 palabras) se siguen recortando; el falso positivo de **62 palabras de narración queda INTACTO** con aviso; cifra inicial recortada; mayúscula sin tocar. No regresión: 25 guiones reales sin cambios y la cadena completa con sha256 idéntico (`4b5995f380824bdf`) · **queda sin cubrir** (documentado, no arreglado): el corte en abreviatura (`Sr.`, `Dr.`, `etc.`) y dos frases descabezadas seguidas (una sola pasada) — con el tope de 28 palabras el daño queda acotado · estado: cierra [FRAG-01]

- [EXIT-01] 2026-08-15 · el-pipeline-falla-ABIERTO (exit 0 con 0 vídeos) · La corrida de producción del 15-ago tomó un chunk de 33,4 min, gastó ~16 min de análisis de hotbar y recodificación por GPU, y murió en el primer bloque de historia porque el proveedor del modelo devolvía `504`/`404`. Terminó con `Pipeline finalizado: 0 videos producidos` y **código de salida 0** · causa: `main()` no devolvía nada y `__main__` hacía `main()` a secas, así que el proceso salía 0 **pase lo que pase** — `daily-run`, un cron o cualquier script encadenado leen eso como corrida exitosa · es la clase [GATE-04] fuera del auditor: *dar la nota perfecta por no haber hecho nada*. Y el pipeline es autónomo por diseño, o sea que nadie mira el log en tiempo real: el exit code ES la señal · arreglado distinguiendo lo único que separa un caso legítimo de un fallo: **si se llegó a INTENTAR**. Con el pool vacío, 0 vídeos es una corrida legítima (exit 0); con `intentos>0 and not success`, exit 1 y un ERROR explícito · verificado por ejecución con los dos caminos: pool vacío → `exit 0`; chunk tomado + `produce_video` lanzando → `exit 1` · estado: pendiente (>=2)

- [CHUNK-01] 2026-08-15 · trabajo-de-ingesta-destruido-por-un-fallo-aguas-abajo · En la misma corrida, `take_chunk` sacó `pool_0001.mp4` del pool (`Removido del pool`) **antes** de generar la historia. Al fallar la historia, el `finally` borró el chunk (`os.remove(chunk_path)`) y el pool quedó **vacío**: `pool/ 0 ficheros`, `temp/ sin chunk`, pese a `--keep-temp` · coste medido: 13.207 MB recodificados a 3.522 MB y 33,4 min de gameplay analizado frame a frame, ~16 min de reloj, tirados por un fallo de un servicio externo · **no se perdió grabación** porque el original sigue intacto en `D:/YOUTUBE_media/input` (la ingesta no borra el input), así que es recuperable re-ingestando; pero el diseño no lo garantiza: el mismo camino con un `input/` ya consumido destruiría el material · pregunta de diseño abierta (NO decidida aquí): ¿devolver el chunk al pool cuando la producción falla, o conservarlo en `temp/` bajo `--keep-temp` como hace con el resto de artefactos? · estado: pendiente (>=2)

- [INSTR-08] 2026-08-15 · veredicto-por-ventana-sin-mirar-la-cobertura-de-esa-ventana · El banco de anclaje daba veredicto a una ventana con solo exigir `len(emparejadas) >= 3`, un mínimo ABSOLUTO. Medido a escala de producción (27,3 min, 171 ventanas, síntesis y alineación frescas): la ventana `t=811,97` (32 palabras, **18 emparejadas = 56%**) salió con `mediana −2,409 s`, con diferencia el peor número de toda la corrida y con pinta de defecto grave nuevo · **era FALSO**. Refutado con dos instrumentos que no dependen de Whisper: `silencedetect` pone **6,45 s de voz dentro de los 7,10 s** que ocupan los subtítulos, y el `SentenceBoundary` de edge-tts arranca en **811,97**, coincidiendo con el primer cue al milisegundo. El emparejador enganchaba ocurrencias lejanas en el 44% sin emparejar: [INSTR-02] reencarnado · lo peligroso es *dónde* estaba el agujero: la cobertura **global** era 96,4% y no avisaba de nada, porque el defecto es **local**. Y las dos salidas posibles eran igual de malas — juzgar la ventana con ruido, o saltarla en silencio, que es fallar abierto ([GATE-04]) · arreglado con `COBERTURA_VENTANA_MIN = 0.70` y, sobre todo, **declarando en voz alta las ventanas NO EVALUABLES** en vez de omitirlas: una ventana sin cobertura no es sana, es SIN MEDIR · verificado que no mueve ningún veredicto previo: v004 `1 → 0` rotas y 18 medibles; 11-ago `2 → 0` rotas, 0 empeoran, invariantes intactos, y ninguna ventana de ≥5 palabras cae por debajo del umbral en los dos corpus · **casi cuesta una sesión entera persiguiendo un fantasma**: el número era plausible, grande y estaba en la dirección que uno teme · estado: pendiente (>=2)

- [MODELO-01] 2026-08-15 · un-solo-proveedor-es-un-punto-unico-de-fallo · La corrida de producción murió con **0 vídeos** porque el proveedor del único modelo configurado (Nvidia, vía OpenRouter) devolvió `504` y luego `404 "Provider returned error"`. Una hora después el mismo modelo respondía `200`: era transitorio · el coste no fue la petición perdida sino **lo que ya se había gastado antes**: 16 min de análisis de hotbar y recodificación por GPU, más el chunk (ver [CHUNK-01]). Un pipeline autónomo por diseño no puede quedarse sin salida porque un proveedor tenga un mal rato · arreglado con **cadena de modelos** en `_call_openrouter` (la costura única): principal + `fallback_models`, y solo se baja cuando el anterior AGOTA sus reintentos, así que **el camino feliz no cuesta ni una petición extra** (verificado: 1 petición). Peor caso `1x5 + 2x2 = 9` en vez de 5, y solo cuando todo va mal · lo que hace seguro bajar a un modelo NO validado contra el prompt real no es confiar en él —`inclusionai/ling-3.0-flash:free` se descartó por tragarse 543 palabras como título— sino que `_validar_salida`, `_parse_title_and_speech` y `_detectar_basura` siguen en vigor y lo rechazan igual (§17) · **dos defectos que destapó el propio test**, ninguno visible leyendo el código: (a) `504` y `522` **no estaban** en la lista de códigos transitorios (`408,429,500,502,503,520,524`), así que un 504 abortaba **sin reintentar ni una vez** — y 504 es justo lo que devolvió el proveedor; (b) `devolver_al_pool` usaba `shutil.move` sin que `gameplay_pool` importara `shutil`: `NameError` con `compileall` **limpio**, la clase [MAIN-01] otra vez · verificado por ejecución los 3 caminos de la cadena (principal cae → baja y devuelve; todos caen → `RuntimeError` nombrando los 3; camino feliz → 1 petición) · estado: pendiente (>=2)

- [DISCO-01] 2026-08-15 · la-ingesta-necesita-17-GB-y-la-doc-decia-12 · La corrida de producción murió con `No space left on device` a mitad de la recodificación, con **16 GB libres** al arrancar. Causa: `clean_gameplay` escribe en `temp/` una copia del vídeo a **TAMAÑO COMPLETO** (13,8 GB para un fuente de 12,9 GB) incluso cuando el vídeo es *"100% gameplay"* y solo recorta inicio y final; sumada al pool recodificado (2,9 GB), una corrida necesita **~17 GB de scratch**, no los ~12 que repetían `sessions-log` y los seeds · el número de la doc venía de mirar el resultado (el pool), no el pico (la copia intermedia) · daño colateral, que es lo grave: ffmpeg dejó un **`pool_0002.mp4` TRUNCADO de 2,9 GB sin `moov atom`** dentro del pool. `add_to_pool` lanzaba la excepción pero **no borraba su propia salida a medias**, así que el fichero se quedó en la cola de trabajo ensuciando cada corrida posterior con un WARNING por lectura, y `take_chunk` acabó reventando con un **traceback sin capturar** al medir el chunk que salió de él · arreglado por los dos lados: (a) `temp_dir` a `D:` (702 GB libres), que quita el límite de raíz; (b) `add_to_pool` **borra la salida truncada** cuando ffmpeg falla, y además **verifica que el fichero es legible aunque ffmpeg diga `returncode 0`** — la misma lección del demuxer `concat`, que terminaba sin error y nunca funcionó · verificado por ejecución con los dos casos: recodificación fallida que deja salida parcial → `RuntimeError` y pool **vacío**; `returncode 0` con fichero ilegible → `RuntimeError` y pool **vacío** · queda apartado `pool/pool_0002.mp4.CORRUPTO` (2,9 GB) por si Diego quiere inspeccionarlo; el gameplay original está intacto en `D:` · estado: pendiente (>=2)

- [ANCLA-07] 2026-08-15 · palabras-colapsadas-que-el-guardia-de-aplastada-NO-ve · Primera producción real completa tras [ANCLA-06] (33,6 min, 6224 palabras, 312 ventanas de anclaje, 54 shorts). El auditor la marcó **NO PUBLICABLE** por una zona de ~12 s: `peor tramo −1,640 s en t=161,94`, `5,84 s de voz sin subtítulo en 8 tramos (peor 1,48 s en t=162,2)`, `racha de 20 palabras ilegibles` · medido sobre el `.ass`: **33 cues de 6203 (0,5%) con duración ≤0,051 s**, concentrados en DOS rachas —`t=161,04` con **20 palabras seguidas** y `t=166,78` con 8— más dos huecos de **3,82 s** (desde t=162,0) y **2,48 s** (desde t=172,8). El resto del vídeo está sano · firma: las 20 palabras miden **0,050 s EXACTOS cada una, independientemente de su longitud** (`Y` y `TRANSFERENCIA` idénticas), que es el `paso_min = 0.05` de `_enforce_monotonic` abanicando palabras que llegan **colapsadas en el mismo instante**. O sea: 20 palabras en 1,0 s = 1200 wpm, ilegible, y detrás 3,8 s de voz sin nada en pantalla · **el guardia `aplastada` no lo caza**: mide `span` sobre las palabras CRUDAS antes del anclaje, y aquí el colapso lo produce (hipótesis a confirmar, no medida: las crudas no se persistieron) el reescalado `scale = s_dur / span` de la rama de TRASLACIÓN, aguas abajo del guardia. Es la misma familia que [ANCLA-06] por el lado contrario: confiar en un ritmo de Whisper que no es una medida · **NO es regresión de [ANCLA-06]**: las 2 únicas ventanas que entraron en la rama modificada fueron `t=1573,08` y `t=1967,86`, ninguna en la zona dañada, y el banco ya había probado que fuera de esa rama la salida es idéntica bit a bit · lo que SÍ validó esta corrida de [ANCLA-06]: la rama nueva se ejerció por primera vez en producción, **2 veces sobre tramos de voz medidos y 0 A CIEGAS** · estado: pendiente (>=2)

- [GATE-07] 2026-08-15 · el-auditor-midio-los-shorts-EQUIVOCADOS · La corrida generó 54 shorts, pero el auditor intentó medir el sincronismo de `short_001` y `short_002` —de una corrida ANTERIOR— y falló con `sin short_00X_subs.ass, NO se ha medido su sincronismo`, elevándolo a `FALLA cobertura de sincronismo` y contribuyendo al veredicto NO PUBLICABLE · causa: coge los primeros shorts del **directorio**, que acumula tandas viejas, en vez de los de **esta corrida**; sus `.ass` ya no existen porque `temp_dir` se movió a `D:` ([DISCO-01]) · el comportamiento del auditor es el CORRECTO —se negó a decir OK sobre algo que no pudo medir, que es justo lo que [GATE-04] vino a imponer— pero apuntó al objetivo equivocado, así que el veredicto mezcla un defecto real con un artefacto de medición · relacionado con [GATE-05] (el auditor medía el DIRECTORIO de shorts, no la corrida), que se arregló para los artefactos pero no para el sincronismo · estado: pendiente (>=2)

- [LEDGER-01] 2026-08-15 · id-reutilizado-entre-sesiones-paralelas · Asigné `[GATE-06]` a un incidente el 15-ago sin ver que **otro track ya había usado ese id el 14-ago** (`check-acoplado-a-una-linea-de-log-que-el-fix-dejo-de-emitir`). Dos defectos distintos compartiendo id · no es cosmético: **`/optimize` promueve una clase a REGLA con ≥2 incidencias del mismo id**, así que una colisión se cuenta como "la clase reincidió" y dispararía una promoción sobre dos bugs que no tienen nada que ver — justo el sobreajuste que el mecanismo existe para evitar · causa: con 3 sesiones escribiendo el mismo `incident-ledger.md`, el siguiente id libre se elige leyendo un fichero que otra sesión está ampliando a la vez. Es [PARALELO-01] aplicado al ledger en vez de al working tree · corregido renombrando el mío a `[GATE-07]` (verificado: 1 sola aparición, y las referencias en `sessions-log.md` actualizadas) · regla operativa barata para la próxima: antes de asignar un id, `grep -oE "^- \[PREFIJO-[0-9]+\]" .claude/incident-ledger.md | sort -u` y coger el siguiente — no fiarse de la memoria de la sesión · estado: pendiente (>=2)

- [GATE-06] 2026-08-18 · **ARREGLADO** (la entrada de arriba queda cerrada) · `cierre_narrativo()` acepta ahora también el marcador del camino de un solo bloque (`"cabe en un solo bloque"`), que es el hecho determinista SIMÉTRICO al que ya comprobaba en el multi-bloque: allí se verifica que el desenlace **se pidió**; aquí, que se pidió la historia **COMPLETA con desenlace en una sola llamada**. Se rechazó explícitamente el arreglo "que reconozca el desenlace escrito en línea" **leyendo el guion**: eso es el juicio que §18 prohíbe y habría convertido el check en un gate que pasa siempre (3.ª repetición de [GATE-04]/[GATE-05], fallar ABIERTO) · evidencia A/B contra casos de resultado conocido, 0 peticiones: sobre los **8 tramos reales** de `pipeline.log` cambia **1 de 8** (`video_008` False→True, que es el falso positivo; `video_001..007` idénticos), y sobre 5 casos sintéticos el multi-bloque sin pedir final **sigue en False**, el tramo vacío **sigue en False**, y sin log / sin tramo siguen devolviendo `None` (nunca se leen como "sano", §16) · efecto colateral MEDIDO y no anticipado por el SEED: `modules/utils.py:104 _HUELLA_FUENTES` incluye `scripts/audit_run.py`, así que el fix cambió `huella_auditor()` **bd2d8329f791 → 0e36ed4b5fd1** y caducó el veredicto de `video_003`; se re-auditó para devolverle un veredicto válido · estado: cerrado

- [CONFIG-01] 2026-08-18 · guardar-desde-la-UI-destruye-el-fichero-y-el-backup-que-promete · `dashboard.py:1288-1293` guarda `config.yaml` con `yaml.safe_dump`, que **no conserva comentarios**: los ~70 de calibración (historial completo de `target_wpm` con las cuatro re-medidas sobre audio, el porqué de `temp_dir` en D: [DISCO-01], la cadena de modelos de reserva [MODELO-01]) desaparecen al pulsar Guardar · agravante: el `.bak` que la UI promete **se sobrescribe en cada guardado**, así que el flujo documentado en el guion de vídeo —bajar `target_duration_min` a 150, grabar, restaurar 1200— lo guarda **dos veces** y deja el `.bak` conteniendo ya la versión sin comentarios: la única recuperación real es `git checkout -- config.yaml` · **no disparado**: detectado antes de ejecutarlo, el pre-flight del guion se corrigió para editar a mano y restaurar con git · lo que enseña: un backup que se sobrescribe en el segundo uso no es un backup para un flujo de dos pasos, y el flujo de dos pasos era el documentado · estado: pendiente (>=2)

- [GATE-08] 2026-08-18 · toda-corrida-lanzada-desde-el-dashboard-nace-inauditable · `dashboard_runner.build_command` (líneas 22-47) acepta `video_path`, `style`, `dry_run`, `skip_ingest` y `no_shorts`, pero **nunca pasa `--keep-temp`**, así que `main.py` llama a `cleanup_temp` → `shutil.rmtree(temp_dir)` en toda corrida con éxito · consecuencia doble: (a) el vídeo recién producido **no se puede volver a auditar jamás** —sin `.ass` ni `_story.txt` no hay nada que medir— y el dashboard, sin veredicto, no lo ofrece para subir; (b) la corrida **destruye los artefactos de vídeos ANTERIORES** que compartan `temp_dir`: hoy `D:/YOUTUBE_media/temp` tenía los últimos de `video_003` (los únicos re-auditables de las tres producciones, porque los de `video_002` ya se perdieron así) · es la misma clase que [GATE-01] (*"sin `--keep-temp` no hay nada que medir"*), que se resolvió en la ruta de CLI y **no se propagó al gemelo del dashboard**, que es por donde Diego lanza de verdad · mitigado hoy copiando `video_003_*` a `D:/YOUTUBE_media/_preserve_video_003/` (52 MB) antes de grabar · estado: pendiente (>=2)

- [SEED-03] 2026-08-18 · el-SEED-declaró-fuera-de-cámara-el-plano-que-el-guion-SÍ-graba · `SEED_grabar_video_portafolio.md` sostiene su análisis de [ANCLA-07] sobre *"Ningún beat filma la cola de subida ni el veredicto de producción"* y *"el badge rojo del veredicto se pinta en `tab_subir` (dashboard.py:903), que el guion no graba"*. El guion, §10 (5:10, beat **★ del corte hero**), dice literalmente: *"Tab 📤 Subir, el vídeo listo en la cola. **El plano es el veredicto del auditor junto al vídeo**"* · el SEED verificó bien la MITAD comprobable en el repo (dónde se pinta el badge: `dashboard.py:903`, correcto) y no comprobó la otra mitad, que vivía en el documento upstream · daño potencial: su plan ponía el fix de [GATE-06] como paso 0, y ese fix cambia `huella_auditor()` → habría convertido el mejor plano de §10 (un rojo real con 3 defectos medibles) en tres tarjetas de *"Veredicto caducado"*, sin que nadie lo relacionara · lo destapó el **agente ciego** del panel, que derivó el plan leyendo el guion sin ver el SEED; los 3 críticos que sí lo leyeron confirmaron la premisa falsa pero ninguno la habría buscado · 2.ª incidencia de la clase de [SEED-02] (el SEED afirma un hecho sobre territorio que no verificó) · estado: pendiente (>=2)

- [COMA-05] 2026-08-18 · guardia-con-el-umbral-por-ENCIMA-del-mecanismo-que-vigila · `modules/script_generator.py:314 _PUNTUACION_P90_MAX = 30` rechaza un guion que edge-tts no podrá narrar sin inventarse respiraciones, y se llama en las DOS rutas de generación (`:430` y `:692`) y en shorts. Pero edge-tts respira cada **~21 palabras** (medido: en `video_009` las 12 pausas inventadas caen a 18-23 palabras del último signo, mediana 21), así que un corte en 30 es **inerte por construcción**: no disparó en **ninguna** de las 9 corridas del `pipeline.log` · disparo: el modelo entregó **0 comas en 714 palabras** (su densidad cruda es bimodal, n=12: `0.00 0.00 1.96 4.41 … 9.10`), `_ensure_breathing_commas` solo pudo insertar 13 —solo sabe hacerlo delante de 11 conectores, y **6 de los 19 tramos peligrosos no contienen ninguno**— y la narración salió con **16.8 pausas inventadas por 1000** contra un máximo de 12.0 · **es [GUARD-01]/§17 otra vez, en su tercera forma**: no es que el guardia no se llame (GUARD-01) ni que mire solo la cabecera (BASURA-01), es que su **constante** está calibrada fuera del rango del defecto · arreglado bajando a **21**, en DOS pasos y el primero fue mío y estaba mal: puse 24 porque era el hueco entre el máximo de los sanos (23) y el roto (28), declarando que 24-27 no estaba observado — y la corrida siguiente cayó **justo en p90=24** y midió **12.0 pausas/1000**, el máximo exacto del auditor. Con ese dato la curva queda monótona y sin huecos: `p90 17-18 → 0.0` · `23 → 3.3` · `24 → 12.0` · `28 → 16.8`, y **21 es el único corte que separa 0.0 de todo lo demás** (y coincide con el mecanismo). A/B final sobre los 8 guiones de resultado conocido: acepta los 5 de 0.0 pausas y rechaza los 3 de >0, **cero falsos en ambos sentidos** · verificado en producción del fixture: la corrida siguiente dio **0 pausas inventadas y 6.81 comas/100** (`video_012`, `VEREDICTO: sin defectos MEDIBLES`) · lección: **elegir el umbral en un hueco no observado es elegirlo a ojo**; hizo falta gastar una corrida para que el hueco dejara de serlo · coste ~+0,2 peticiones/vídeo, y agotar reintentos NO aborta (`_es_fallo_solo_puntuacion` acepta el mejor intento) · **gemelo pendiente**: `shorts_generator.py:16` importa el MISMO umbral, y `audita_shorts` no mide `pausas_inventadas` (se abstiene por debajo de 500 palabras), así que en shorts este eje **no lo mide nadie** · estado: pendiente (>=2)

- [COHER-01] 2026-08-18 · el-check-de-coherencia-no-distingue-PARÁFRASIS-de-resolución-ausente · `scripts/audit_run.py:399 coherencia_titulo_cuerpo` marcó `NO PUBLICABLE` sobre `video_009`, cuyo título prometía *«el juez descubrió la estafa y lo mandó a la cárcel»* — y el cuerpo **narra la resolución entera**: *«el magistrado leyó el dictamen pericial… la firma no coincidía»*, *«las grabaciones llegaron al juez»*, *«leyeron la sentencia de tres años de prisión»* · el emparejado es por **raíz de 5 caracteres**, así que `prisión`≠`cárcel`, `sentencia`≠`mandó` y `dictamen pericial`≠`estafa`: ratio 40% con `truncado narrativo` en **OK** y el log diciendo *"esta historia no se truncó"* · **ojo con el diagnóstico fácil**: un primer análisis lo atribuyó a que el umbral 0.60 se calibró sobre guiones de 5300 palabras y el fixture produce 600, y montamos el control de longitud para demostrarlo — **el control lo REFUTÓ**: al conservar título + últimas N palabras el ratio cae porque el recorte se lleva de verdad los stems (`cambió` en la posición 2673, `encontré` en la 4887), no por longitud. La causa es la sinonimia, y solo se vio LEYENDO el texto · arreglado acotando el check a su mecanismo: **bloquea solo si hubo truncado** (sin truncado, la causa de [TRUNCA-01] no puede haber ocurrido) y avisa en el resto; verificado: `video_009` → AVISO, `video_008` (truncado 21%, resolución de verdad ausente) → sigue FALLA · lo que enseña: **el instrumento que casi arreglamos por la razón equivocada habría quedado "arreglado" y roto**, y el que lo evitó fue reproducir el control en vez de aceptarlo del informe · estado: pendiente (>=2)

- [SHORTREP-01] 2026-08-18 · la-ventana-anti-repeticion-se-ensancho-en-el-CONSUMIDOR-y-no-en-el-SEMBRADOR · `modules/shorts_generator.py` consume `avoid[-40:]` desde `c9ac16d` (10-ago, *"fix ventana anti-repeticion de shorts: 12 -> 40 titulos"*), pero quien SIEMBRA esa lista con los títulos ya en disco es `main.py:141`, y seguía en `sorted(glob(...))[-8:]` · efecto: la protección ENTRE corridas era de **8** títulos, no de 40, y el ensanche no servía de nada salvo dentro de la propia tanda · **disparado hoy**: `short_032` (19:44) repite el argumento de `short_017` (17:43) — *"Mi **Primo** Robó Mi Boleto Ganador Y Mi **Abogado** Lo Esperaba En La Puerta"* vs *"Mi **Hermano** Robó Mi Boleto Ganador Y La **Lotería** Lo Descubrió Al Instante"*: mismo objeto robado, mismo parentesco varón, mismo desenlace policial y la MISMA CTA byte a byte. Es la firma exacta que `CLAUDE.md` documenta del incidente de los 4 shorts idénticos · A/B: reconstruida la siembra sobre los 31 títulos en disco, con `[-8:]` entran `short_025..032` y **`short_017` NO**; con la ventana correcta entran los 31 · **el gate no puede verlo**: `OK títulos únicos: 27/27` compara cadenas exactas y dos historias iguales con títulos distintos pasan siempre — lo cazó el `output-audit` comparando ARGUMENTOS, no cadenas · clase [PATH-02]/[LOG-02] (fix no propagado al gemelo), 3.ª incidencia · arreglado atando ambos extremos a una constante única `AVOID_VENTANA` en `shorts_generator`, para que no puedan volver a desincronizarse · ⚠️ límite declarado: pasar los títulos SUBE la probabilidad, no la garantiza — es prompt, no un `if` (§17); la garantía dura seguiría siendo un check de argumento, que no existe · `CLAUDE.md` dice *"se limita a los 12 últimos"* y la tabla del guion de portafolio dice 40: **las dos cifras estaban desactualizadas** · estado: pendiente (>=2)

- [TRUNCA-02] 2026-08-19 · **CERRADO** (la entrada de arriba queda resuelta) · `_truncate_to_words` conserva el principio y las últimas N palabras como desenlace, así que lo que descarta es la **RESOLUCIÓN** — y el TÍTULO la promete, y ese título va a la miniatura y a la intro · tasa base medida en 9 corridas (18-19 ago, objetivo 623): el modelo escribió 665, 714, 854, 909, 919, 938 palabras, y **las 4 que pasaron de 747 (623 × 1,2) publicaron un vídeo que promete una escena que no narra**: `video_004` (938 → descarta frases 11-19 de 25, coherencia **20%**), `video_006` (854 → frases 16-27 de 36, **57%**), `video_011` (919 → frases 8-15 de 21, **40%**) · arreglado **regenerando en vez de recortar**: el camino de un bloque pide otra historia hasta 2 veces si se pasa del tope, y solo si se agotan conserva el intento MÁS CORTO y deja decidir al truncado — **el peor caso es exactamente el comportamiento anterior y nunca aborta** · A/B offline con un doble, 0 peticiones: `[665]` → 1 petición sin truncar · `[854,700]` → 2 peticiones sin truncar · `[938,919,854]` → 3 peticiones, conserva 854 · **verificado EN PRODUCCIÓN 40 minutos después del commit**: `video_007` escribió 909 → `se REGENERA la historia (intento 2/3)` → 718 → `OK truncado narrativo: esta historia no se truncó` → `VEREDICTO: sin defectos MEDIBLES` · el `1.2` pasa a ser `_SOBREPASO_MAX_FACTOR`, leído por el bucle y por el truncado · coste: hasta +2 peticiones cuando dispara (~4 de 9 corridas) · estado: cerrado

- [GATE-05b] 2026-08-19 · el-auditor-medía-los-shorts-del-DIRECTORIO-y-no-los-de-la-corrida · `main.py:_auditar_salida` pasaba `--shorts 2` **sin decir cuáles**, así que `audita_shorts` acotaba por nombre y cogía `short_001` y `short_002` (del 11-ago), cuyos `.ass` ya no existen → `FALLA cobertura de sincronismo: se pidió medir 2 short(s) y NO se pudo medir ninguno` sobre una corrida cuyos 4 shorts **sí tenían su `.ass` en temp** · el vídeo salía marcado NO PUBLICABLE por un fallo que no era suyo · el flag `--shorts-stems` existía desde [GATE-05] y **nadie lo usaba desde `main.py`**: es la 2.ª incidencia de la misma clase, y la 1.ª se arregló solo en el lado del auditor · arreglado haciendo que `generate_shorts_for_video` devuelva los STEMS en vez de un contador · verificado re-auditando `video_005` sin regenerar nada: `NO PUBLICABLE — 1 problema(s)` → `acotado a --shorts-stems: 2 stem(s)` → `short_059 medio 0.065s`, `short_060 medio 0.035s`, cobertura 2/2 → **`sin defectos MEDIBLES`** · estado: pendiente (>=2)

- [LOG-03] 2026-08-19 · el-log-que-el-dashboard-ENSEÑA-nacía-en-cp1252 · `dashboard_runner.launch_run` abría el fichero de log con `encoding="utf-8"` pero **no le fijaba la codificación al proceso hijo**, que en Windows toma cp1252 del locale. El auditor imprime tildes, `«»` y `→`, así que **cada una de esas líneas reventaba el StreamHandler** y Python escribía un `UnicodeEncodeError` con su traceback ENTERO dentro del log · medido: la corrida de `video_004` metió **~20 tracebacks** entre las líneas del veredicto, y ese log es justo el que la pestaña 📡 Progreso muestra en vivo — el plano de los beats 1:45 y 2:48 del vídeo de portafolio · arreglado con `PYTHONUTF8=1` + `PYTHONIOENCODING=utf-8` en el `env` del `Popen`; la corrida siguiente dio **0 tracebacks** · misma clase que el `PYTHONUTF8=1` que ya necesitaba la tarea programada del escaneo: **se sabía, y no se propagó al lanzador** · estado: pendiente (>=2)

- [GATE-08] 2026-08-19 · **CERRADO** · `build_command` no pasaba nunca `--keep-temp`, así que `cleanup_temp` borraba el `.ass` y el `_story.txt` al terminar: toda corrida lanzada desde el dashboard nacía **imposible de auditar** (y sin veredicto el dashboard no la ofrece para subir), y además **destruía los artefactos de vídeos anteriores** que compartieran `temp_dir` · arreglado pasándolo SIEMPRE · verificado: `Temporales CONSERVADOS en D:/YOUTUBE_media/temp (--keep-temp)` y los 4 shorts de la corrida medidos · estado: cerrado

- [COHER-02] 2026-08-20 · el-fix-de-[TRUNCA-02]-apagó-el-detector-de-[TRUNCA-01] · el arreglo de [COHER-01] (18-ago) hizo que la coherencia título/cuerpo **bloqueara solo si hubo truncado** (`audit_run.py:705 elif not _frac_trunc`), con la premisa escrita *"sin truncado la causa de [TRUNCA-01] no puede haber ocurrido"*. La premisa es **falsa**, y el fix de [TRUNCA-02] del día siguiente la volvió **dominante**: al hacer raro el truncado, mandó casi todo el tráfico por la rama que no bloquea · **disparado**: `video_005` y `video_007` de la grabación del 19-ago quedaron `ok:true` → **publicables en el dashboard**, que cablea el botón a `bool(auditoria["ok"])`, con un `AVISO` de la clase que el check existe para cazar (`pipeline.log:6855` → 25%, raíces ausentes `dejo, nada, testa`). `video_007` promete en su título —y por tanto en la MINIATURA y la INTRO— *«el testamento final la dejó sin nada»* y en sus 589 palabras **no hay ningún testamento**: resuelve por sentencia judicial · agravante: fallaba **ABIERTO**, porque sin `pipeline.log` `_frac_trunc` es `None` y `not None` mandaba también al AVISO ([GATE-04], 4.ª incidencia) · lo destapó el **agente ciego** de `/seed-review`, que no vio el SEED; el SEED daba [TRUNCA-02] por cerrado apoyándose justo en esos dos veredictos · **el umbral NO era la palanca, y está medido**: con verdad-terreno establecida LEYENDO 11 cuerpos, el ratio de raíces no separa (0,40 es falso positivo dos veces, 0,29 y 0,50 son verdaderos) y la hipótesis alternativa *"falta un sustantivo concreto"* bloquea **4 de 5 verdaderos Y 4 de 5 falsos**: cero separación. El cuerpo narra la resolución con otras palabras (`cárcel`→`prisión`, `perdió su licencia`→`asesoría fiscal sin firma`, `las pruebas`→`la filtración de los correos`) · arreglado con §18: el léxico pasa a **pre-filtro** y quien juzga es un LLM (`_juez_llm_coherencia`), mismo patrón que `trend_advisor.classify_channels` ante la misma forma de problema. La REGLA no se delega —la fijó Diego y va en el prompt: resolver por una vía distinta de la que el título nombra cuenta como incumplido— y el caso no juzgable **falla CERRADO** · calibrado contra 10 casos de resultado conocido: **9/10**, con **5/5 de recall** en promesas incumplidas; el único desacuerdo era **mi etiqueta**, no el juez (`video_011` no narra ninguna falsificación ni ningún notario que la descubra: `falsific` aparece 0 veces) · constantes medidas, no a ojo: `max_tokens=2500` (con 120 y con 900 el modelo se corta a mitad de su propio razonamiento) y cuerpo acotado a las **700 últimas palabras** · verificado por ejecución: `video_007` **`ok:true` → `NO PUBLICABLE`** con el motivo correcto, y sin regresión, `video_012` sigue **`sin defectos MEDIBLES`** — ahora con el juez rescatándolo explícitamente de un 25% léxico que si no lo habría bloqueado · degenerados (§16): guion vacío / sin frase / sin conector / cláusula de 1 palabra → se abstiene; fichero ausente → excepción **capturada y bloqueante**; sin API key → `None` → bloquea · destapó de paso que `audit_run` **nunca cargaba `.env`** y ahora lo necesita · coste: **+1 petición por vídeo largo** (hasta +2 con el reintento de formato) solo cuando el pre-filtro falla, que hoy es 14 de 18 guiones reales: ~48 → ~49 por vídeo de 30 min · ⚠️ **límite declarado y medido**: el juez **NO es determinista en los casos ambiguos**. Estable 3/3 en `video_007` y en `video_009`, pero `video_005` da **4 NO / 1 SI en 5 repeticiones** — y su `video_005_audit.json` actual en verde es justo el 1 de 5. Su cláusula es un final de título genérico (*"El Testamento Oculto Cambió Todo Para Siempre"*), así que la pregunta está mal planteada desde `_clausula_resolutiva`: **no publicar `video_005` sin re-auditarlo** · estado: pendiente (>=2)

- [QUOTA-02] 2026-08-24 · el-modelo-de-cuota-de-YouTube-era-el-VIEJO-de-Google · `QUOTA_COST` cobraba `search`=100 y `videosInsert`=1600 **unidades de un bote unico de 10.000**, y `QuotaMeter` modelaba ese bote unico. El reparto real de la API v3 son **TRES cupos independientes** — `"a default quota allocation of 100 search.list calls, 100 videos.insert calls, and 10,000 units per day combined for all other endpoints"` (developers.google.com/youtube/v3/getting-started, verificado 24-ago-2026; `videos.insert` cuesta **1** unidad, no 1600) · **el error iba en las DOS direcciones**: (a) `puede_subir` rechazaba la 7.ª subida del dia cuando el limite real son 100 — medido: con 6 subidas cobradas el preflight decia que no, y ahora dice `Quedan 94 subidas de 100`; (b) el cupo de busquedas **no lo vigilaba nadie**, porque su gasto se cargaba a un bote que no es el suyo, asi que nada impedia pasar de 100 `search.list`/dia y comerse un 403 no previsto · lo destapo el orquestador del repo PORTAFOLIO al verificar un claim de la web contra la fuente oficial, no un gate de este repo: **ningun test de aqui podia verlo**, porque todos comparaban contra la misma constante equivocada — es [DOC-01] en su forma pura, un dato externo que caduco en silencio y se propago como verdad desde `CLAUDE.md` hasta el codigo · ⚠️ **el otro agente acerto en el fondo y erro en un detalle**: dijo que `competitor_scout.py:51` era "un comentario, o sea prosa"; es una entrada viva de la tabla que `youtube_uploader.py:299,313` consulta para decidir si deja subir · arreglado con `QUOTA_BUCKET = {endpoint: (cupo, coste)}` + `QuotaMeter` de tres cupos, conservando `QUOTA_COST` (suma de costes homogeneos) y `daily_limit` (compat: siempre significo el cupo de unidades) · verificado por ejecucion: 100 subidas aceptadas contra 6 antes y **0 unidades** consumidas por ellas; 100 busquedas y corte al 101; agotar un cupo **no** bloquea los otros; degenerados (§16) state de otro dia -> todo a 0, state viejo con solo `units` -> los otros a 0, endpoint desconocido -> `KeyError` ruidoso; y la **mezcla entre procesos** (escaneo + subidor intercalados) da `search=4 upload=1 units=53` sin perder ni un cargo, con el `_quota_pending` escalar antiguo migrado como unidades · dashboard 0 excepciones y 8 pestañas · estado: pendiente (>=2)

- [SEC-01] 2026-08-24 · el-dashboard-permitia-escritura-arbitraria-y-escuchaba-en-toda-la-red · auditoria de seguridad previa a hacer PUBLICO el repo. Dos bloqueantes que se combinaban en **ejecucion remota de codigo**: (a) `dashboard.py:362` hacia `os.path.join(input_dir, uploaded.name)` con el nombre de fichero **que manda el cliente**, y Streamlit NO lo sanea (`type=["mp4"]` es filtro de frontend, no de la ruta de subida): un `filename="../modules/utils.py"` escribia sobre un modulo que `main.py` y el propio dashboard importan; (b) `.streamlit/config.toml` no fijaba `server.address`, asi que Streamlit se ataba a `0.0.0.0` **sin autenticacion** — verificado con `netstat`: `TCP 0.0.0.0:8599 LISTENING` — y cualquiera en la LAN tenia la consola entera: lanzar el pipeline, ver prefijos de claves, reescribir `config.yaml`, y **subir videos al canal con el token OAuth ya guardado**. Los cuatro sitios que documentan el arranque (`README.md:38,172`, `CLAUDE.md:170`, `run/SKILL.md:21`) lo omitian, asi que la instruccion del propio repo llevaba al fallo · tres mas, de credenciales: **[H3]** la YouTube API key viajaba en el QUERY STRING (`competitor_scout.py:413`) y `requests` mete la URL completa en el mensaje de sus excepciones, que acaba en `pipeline.log` (`main.py:196`) y **EN PANTALLA** (`dashboard.py:701`) — probado A/B: con la clave en la URL aparece en `str(e)`, por cabecera `X-goog-api-key` **no**; **[H4]** «Guardar config» volcaba el dict FUSIONADO al `config.yaml` **versionado**, asi que horneaba en el fichero del repo las rutas locales y una clave si estuviera en el overlay — **regresion introducida ESE MISMO DIA** por el overlay `config.local.yaml`, el patron de "el fix trae su propio defecto"; **[H5]** el refresh token de larga vida se escribia con permisos por defecto (0644 en un clon POSIX), sin un solo `os.chmod` en todo el repo · arreglado: `basename` + comprobacion de que la ruta resuelta cae dentro de `input/` + extension, `address = "127.0.0.1"`, clave por cabecera, `load_config_raw` para el editor, y el token creado con `os.open(..., 0o600)` ANTES de escribir · verificado por ejecucion: 12 payloads de travesia (`../modules/utils.py`, `..\main.py`, `....//....//main.py`, vacio, `.`, `..`, `clip.mp4.py`) **todos bloqueados** y los legitimos siguen entrando; A/B de la fuga de la clave `True -> False`; guardar la config ya no escribe ni la clave ni la ruta local en el versionado y el overlay queda intacto; dashboard 0 excepciones y 8 pestañas · **lo que NO estaba roto y conviene registrar**: 0 `shell=True`, 0 `eval`/`exec`, `yaml.safe_load` en todos los sitios, timeout en las 5 llamadas de red, locks reales con escritura atomica, y el texto del LLM **nunca** llega a un argumento de FFmpeg (va dentro del `.ass`, con `_escape_ass`) — los tres fallos serios vivian en la capa de OPERACION, no en el pipeline · estado: pendiente (>=2)
