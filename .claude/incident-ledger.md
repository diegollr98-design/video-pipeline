# Ledger de Incidentes

**Append-only.** Registro FACTUAL de incidentes/near-misses (proceso, bugs, verificación fallida…).
Vive en `.claude/` → **NO se auto-carga** (sin coste de tokens por sesión). **No editar entradas pasadas; solo añadir** (más reciente arriba).

**Uso:** materia prima de la auto-mejora. **Solo `/optimize`** promueve una entrada a regla, y solo con **≥2 incidencias independientes** de la misma clase, **o 1 de clase irreversible** (dinero/datos/seguridad/PII).

**Formato:** `- [id] fecha · clase · qué pasó · evidencia · estado`
**Estado:** `pendiente (>=2)` = registrado, sin promover · `promovido -> <regla>` = ya es regla.

<!-- entradas nuevas debajo, más reciente arriba -->

> **Promoción del 2026-08-10 (`/optimize`).** Cuatro clases superaron el umbral y ya son regla:
> · **instrumento no calibrado** (3 incidencias: SYNC-01, INSTR-01, INSTR-02) → `produccion-loop.md` §D, regla madre
> · **régimen de validación equivocado** (3: ANCLA-01, BASURA-01, WPM-01) → `produccion-loop.md` §C, "el techo del gate"
> · **fix no propagado al gemelo** (2: PATH-02, LOG-02) → `decision-making.md` §11, tabla de pares + grep obligatorio
> · **garantía existente pero no aplicada** (2: GUARD-01, BASURA-01; 4.º episodio de §17) → `decision-making.md` §17, 2.º corolario
> Sin promover (n=1, reversibles, ya arregladas): DOC-01, COMA-01, GATE-01. DOC-01 sí motivó **corregir el
> dato caducado en los 6 ficheros donde seguía vivo** — la corrección de un hecho no es una regla nueva.

- [DOC-01] 2026-08-10 · doc-caducada · `CLAUDE.md` §OpenRouter afirmaba "0 créditos comprados, saldo −0,06 USD, 50 peticiones/día" cuando la realidad eran 10 créditos y 1000/día. **Tres revisiones adversariales independientes repitieron el dato falso** como hecho verificado porque estaba escrito en `CLAUDE.md`; el SEED, que decía lo correcto, fue acusado por las tres de tener una premisa falsa · evidencia: `GET https://openrouter.ai/api/v1/credits` → `{'total_credits': 10, 'total_usage': 0.063839}`, `is_free_tier: False`; `CLAUDE.md:380` (antes del fix) · estado: pendiente (>=2)

- [SYNC-01] 2026-08-10 · medición-invalida · Al comparar la versión vieja y la nueva de `_ensure_breathing_commas` usé la métrica `exceso = nº silencios − nº signos de puntuación`. El número de comas insertadas está en el denominador, así que la métrica penaliza automáticamente a la variante que inserta menos comas: dio "5 vs 10 pausas inventadas" cuando los silencios crudos eran **30 vs 31** (indistinguibles). Casi cierro un cambio como regresión por un artefacto de mi propia métrica · evidencia: `ffmpeg silencedetect n=-35dB:d=0.25` sobre las dos síntesis, 420 palabras del mismo texto · estado: **promovido → `produccion-loop.md` §D (regla madre: calibra el instrumento contra un caso conocido)**

- [PATH-02] 2026-08-10 · bug-gemelo-no-revisado · `gameplay_pool._concat_videos:74` escribía rutas RELATIVAS en el fichero de lista del demuxer `concat`. Es el **gemelo exacto** del bug arreglado en `video_cleaner._concat_segments`, y la regla 11 del repo ("bug en un path → revisa los paths análogos") existía ya cuando se aplicó aquel fix. Se dispara desde `take_chunk` con 2+ ficheros en el pool = el caso normal de producción; el fixture E2E tiene 1 solo fichero y nunca lo ejerció · evidencia: repro con 2 clips en `./pool` + lista en `./temp` → `FALLO: Error concatenating`; tras `os.path.abspath` → `OK duration=2.000000`; integración con 3 trozos de gameplay real → chunk de 152,6 s, desvío 0,00 s · estado: **promovido → `decision-making.md` §11 (tabla de pares gemelos + grep obligatorio)**

- [LOG-02] 2026-08-10 · error-ilegible · 4 sitios de `gameplay_pool.py` registraban `stderr[:300]` de FFmpeg, que son el **banner de compilación**: el error real quedaba fuera del log. Es el mismo defecto ya documentado y arreglado en `video_cleaner`, no propagado al módulo hermano · evidencia: el repro de [PATH-02] devolvió `ffmpeg version 8.1-full_build... configuration: --enable-gpl...` en vez de la causa · estado: **promovido → `decision-making.md` §11 (tabla de pares gemelos)**

- [GUARD-01] 2026-08-10 · garantia-en-prosa · Dos garantías estaban escritas y no impuestas: (a) `_validar_salida` no se llamaba en `_generate_continuation`, así que en una historia de 30 min (4-6 bloques) el guardia anti-razonamiento cubría solo el primero; (b) el invariante "`_ensure_breathing_commas` no cambia el número de palabras" vivía en un comentario, sin `assert`, pese a que `main.py` cuenta palabras para arrancar la intro · evidencia: `grep -rn "_validar_salida" modules/` → solo `script_generator.py:253` y `shorts_generator.py:93`; property test nuevo: 0 violaciones en 1.248 comprobaciones · estado: **promovido → `decision-making.md` §17 (2.º corolario: un guardia que existe no está aplicado)**

- [COMA-01] 2026-08-10 · heuristico-incorrecto · `_CONECTORES_LARGOS` insertaba comas gramaticalmente incorrectas en español: partía locuciones ("antes de, que"), separaba verbo y complemento ("amenazando, con destruir") y convertía relativas especificativas en explicativas ("el primer sueldo, que mi padre ganó"), que **cambia el significado**. Además `_CONECTORES_SEGUROS` tenía "aunque" 5 veces de 12 entradas · evidencia: auditoría sobre 7.399 palabras de texto español generado; 68 de 92 inserciones (74%) salían del grupo ambiguo · estado: pendiente (>=2)

- [WPM-01] 2026-08-10 · calibrado-en-regimen-equivocado · `target_wpm: 195` se calibró con clips de 3 min, donde el modelo se pasa y manda `_truncate_to_words`; a 30 min manda su propia capacidad y el ratio real proyectado es 1,21 (≈6 min de gameplay repetido). Además `main.py:80` usaba `story.target_wpm` para dimensionar los SHORTS, así que el cambio 150→195 subió el gasto de 33 a 43 peticiones por vídeo (+30%) **sin reportarse**, contra la regla explícita del repo · evidencia: `pipeline.log` n=10, wpm real 200 en textos cortos vs 160,6 en la única producción larga (5047 palabras / 1885 s) · estado: **promovido → `produccion-loop.md` §C (régimen de validación)**; el valor del knob sigue abierto (n=2: 160,6 y 177,2)

- [GATE-01] 2026-08-10 · gate-destruye-su-entrada · `/eval` no podía medirse a sí mismo: `cleanup_temp` (`utils.py:76`, llamado en `main.py:397`) borra `temp/` al terminar una corrida con éxito, y ahí viven el `.ass` y el `_story.txt` que son **exactamente** lo que el gate mide. La primera corrida del gate solo pudo medirse porque copié los artefactos a mano mientras el pipeline corría. Consecuencia mayor: **ninguna corrida de producción era auditable a posteriori** · evidencia: `ls test_e2e/temp/` vacío tras la corrida; artefactos rescatados en paralelo · arreglado con `--keep-temp` · estado: pendiente (>=2)

- [INSTR-01] 2026-08-10 · instrumento-roto · Mi primera versión de `pausas_fuera_de_puntuacion` en `scripts/eval_sync.py` calculaba el hueco como `siguiente.start - previa.start`, que es la **duración de la palabra**, no el silencio. Reportó **73 pausas fuera de puntuación**; con `siguiente.start - previa.end` son **1**. Segundo error de medición mío en la misma sesión (ver [SYNC-01]): la clase es "métrica plausible no validada contra un caso conocido" · evidencia: `DESPACHO` cue 9.35→9.77 daba "hueco 0,42 s" sin haber silencio alguno · estado: **promovido → `produccion-loop.md` §D (regla madre)**

- [BASURA-01] 2026-08-10 · vídeo-publicable-roto · En la primera corrida de producción real (29,85 min) el modelo escupió a mitad de la historia `"de onean need that the writing plan2:en300 five the 02 0230 the0222 0207- 01=، the 02201 the"`, y eso se **narró y se subtituló**: sale en pantalla del minuto 15:38 al 15:47 de un vídeo publicable. Los guardias existentes (`_validar_salida`, y el `_validar_continuacion` añadido ESE MISMO DÍA) solo inspeccionan la CABECERA de la salida, porque el modo de fallo conocido era el razonamiento al principio; este está enterrado en el cuerpo · evidencia: `temp/video_001_story.txt` palabras 2778-2798; `temp/video_001_subs.ass:2756,2765,2767` (`ONEAN`, `0230`, `0207-`) · arreglado con `_detectar_basura` (ventana deslizante de 60 palabras, ≥3 tokens anómalos DISTINTOS), 0 falsos positivos sobre ~12.700 palabras de español real · estado: **promovido → `produccion-loop.md` §C (régimen) + `decision-making.md` §17 (2.º corolario: el guardia miraba solo la cabecera)**

- [ANCLA-01] 2026-08-10 · desfase-real-nunca-medido · Primera medición de sincronismo a escala de producción (214 frases frente a las ≤19 del fixture): **4 ventanas discretas** con frases enteras ~1 s por detrás de la voz. Causa raíz localizada por `bug-hunter` y validada por el orquestador: `tts_engine.py:458` calcula la traslación de toda la ventana con UNA sola palabra (`offset = s_start - w_first`); si el alineador mete esa primera palabra dentro del silencio previo, el error se propaga rígido a las ~94 palabras siguientes · evidencia: fragmentos frescos de 40 s → 1000-1040 s mediana **+1,050 s** con 90 de 125 palabras >0,5 s, control 300-340 s **−0,110 s**; descartados por medición edge-tts (214/214 anclas a ±0,5 ms), composición (−0,100 s exactos) y `subtitle_builder` (±9 ms) · **fix propuesto, NO aplicado**: el fixture de 3 min no puede verlo · estado: **promovido → `produccion-loop.md` §C (el techo del gate)**; ⚠️ el BUG SIGUE VIVO en `tts_engine.py:458` — bloque B de `SEED_sincronismo_produccion.md`

- [INSTR-02] 2026-08-10 · instrumento-roto · `scripts/eval_sync.py` empareja con `difflib` GLOBAL sobre 5000+ palabras y engancha ocurrencias lejanas donde el texto se repite, fabricando retraso inexistente. Reportó desfase en 1245-1285 s, 1585-1625 s y casi todo 605-645 s; transcripciones frescas de 40 s de esas zonas dan **−0,080 s** (sanas). Infló la media global de 0,072 a 0,153 s y volteó el sesgo. Tercer error de medición propio en la misma sesión · evidencia: tabla de 5 zonas, `validar_veredicto.py` · **pendiente**: emparejado local por ventanas (bloque A de `SEED_sincronismo_produccion.md`) · estado: **promovido → `produccion-loop.md` §D (regla madre)**; el arreglo del script sigue abierto

- [DEDUP-01] 2026-08-10 · vídeo-publicable-roto · La primera producción real **narra dos veces el mismo párrafo**: 83 palabras repetidas literalmente en `temp/video_001_story.txt` (posiciones 2687 y 2792) y 72 n-gramas de 12 duplicados. `script_generator.py:492` concatenaba `story + "\n\n" + continuation` sin comparar el bloque nuevo con lo ya escrito, y `_validar_continuacion` juzga el bloque **aislado**, así que ningún guardia podía verlo. Efecto colateral medido por el agente ciego: al quedar el bloque 2 cortado sin puntuación terminal, el separador de frases fusionó el tramo en una frase de 391 palabras. Ni el SEED v1 ni las tres revisiones previas lo nombraban · evidencia: `python - <<'PY'` sobre `temp/video_001_story.txt` → `n-gramas de 12 repetidos: 72`, `primer tramo repetido: pos 2687 y pos 2792, longitud 83 palabras` · arreglado con `_strip_duplicated_opening` (≥12 palabras consecutivas idénticas, solo al arranque del bloque); validado: corta 84 palabras en la costura real, **0 falsos positivos en 200 costuras arbitrarias** del mismo texto español y en los 50 shorts · estado: pendiente (>=2)

- [ANCLA-02] 2026-08-10 · diagnostico-incompleto · El fix que proponía `SEED_sincronismo_produccion.md` v1 para ANCLA-01 ("si el paso de la 1.ª a la 2.ª palabra supera un umbral, adelanta el resto de la ventana ese exceso") **no podía funcionar**: el paso no predice el error, y hay inversión de rango — paso 2,11 s → error 1,07 s, pero paso 1,98 s → error **1,48 s**. Con K=0,55 quedan residuos de −0,49 a +0,33 y 6 falsos positivos (vocativos con coma = pausa legítima); con K=1,20, residuos de +0,16 a +0,98. La razón es que hay **DOS modos de fallo** y el paso los conflaba: (1) ancla corrupta —Whisper mete la 1.ª palabra en el silencio previo— y (2) **silencio inventado DENTRO de la ventana**, donde el ancla es correcta y lo roto es el ritmo interior (`'El silencio que siguió fue absoluto.'`, 6 palabras y 2,91 s según edge-tts, salió con 2,09 s entre la 1.ª y la 2.ª palabra cuando la voz las dice a 0,36 s). Además el fix propuesto generaba **solapes** de subtítulo que nada cortaba · evidencia: `scripts/anchor_bench.py bench`; barrido del umbral interno sobre 214 ventanas contra transcripción independiente · arreglado con offset = mediana de residuos del vecindario (`ANCLA_TOL=0.40`) + cierre de huecos internos (`ANCLA_HUECO_MAX=0.80`) + `_enforce_monotonic`: palabras desincronizadas **204 → 20**, vídeo afectado **60,3 s → 7,2 s**, p95 **1,010 → 0,286 s**, sesgo **+0,005 → −0,040 s**, solapes **2 → 0** · estado: pendiente (>=2)

- [INSTR-04] 2026-08-10 · acusacion-no-reproducible · **[INSTR-02] atribuyó a `scripts/eval_sync.py` un fallo que ese código no tiene.** Decía que su `difflib` global "infló la media de 0,072 a 0,153 s y volteó el sesgo", y el SEED ordenaba reescribir el JSON de la corrida larga por estar "mal". Medido A/B sobre los mismos artefactos, el emparejador global y uno local por ventanas dan **resultados idénticos** (5062 pares, |err| medio 0,153 s, sesgo +0,003 s) y **los 6 veredictos de zona coinciden** con las transcripciones frescas de 40 s, incluidas las tres que INSTR-02 daba por falsas. El 0,072 era el baseline del **fixture de 3 min**: se comparó con producción sin decir que son dos regímenes distintos, y la subida real la causa ANCLA-01 (204 palabras a ~+1 s), no el instrumento. El script citado como evidencia (`validar_veredicto.py`) ya no existe en el repo · evidencia: A/B pegado en la sesión; `data/eval/2026-08-10-produccion-real.json` **conservado intacto** y la re-medición escrita aparte en `-v2.json` · consecuencia útil: el emparejado local se adoptó igual (no puede enganchar repeticiones lejanas, coste 0) y se añadió al gate la métrica que sí ve esta clase de fallo — `peor_tramo` (mediana del tramo de 40 palabras más desincronizado): **+1,110 s en t=690 s** donde la media decía 0,153 s y el sesgo +0,003 s · estado: pendiente (>=2)

- [ANCLA-03] 2026-08-11 · fix-que-rompe-otra-cosa · La primera versión del anclaje robusto (mediana del vecindario) descartaba el ancla SIEMPRE que el residuo se apartaba de sus vecinos. Eso rompió **3 de 16 shorts** de producción: `short_008` con un tramo entero a **−1,98 s** de la voz y `short_004` con sesgo **+0,132 s** y 15 de 122 palabras >0,5 s tarde, mientras el vídeo largo mejoraba. La causa: un residuo grande tiene DOS orígenes opuestos —ancla corrupta (outlier AISLADO, vecinos de acuerdo) y **deriva del alineador** (residuos que crecen en ventanas SEGUIDAS: −3,47 y luego −5,39)— y en el segundo el ancla de edge-tts es la CURA, no el problema · evidencia no circular: `silencedetect` sobre el audio del short da silencios que acaban en **39,596 s y 45,918 s**, justo donde edge-tts arranca esas frases (39,41 / 45,75), mientras Whisper las ponía en 42,88 y 51,14 · arreglado exigiendo que el outlier sea aislado; medido después: ayer 204 palabras → 15, hoy peor ventana 7,43 → 1,45 s · **lección**: el vídeo largo daba verde a un fix que rompía los shorts; sin medir el gemelo se publica igual · estado: pendiente (>=2)

- [ANCLA-04] 2026-08-11 · alineador-aplasta-ventana · En la corrida del 11-ago, ~40 palabras salieron hasta **7,4 s POR DELANTE** de la voz. Whisper metió 44 palabras en **4,98 s** cuando edge-tts dice que esa frase dura **13,49 s** (530 wpm, imposible de pronunciar): el ancla las devolvía al inicio correcto pero seguían apelotonadas, así que el subtítulo se vaciaba en 5 s mientras la voz tardaba 13. El código contemplaba el caso contrario (Whisper más largo que la ventana → comprimir) pero no este · evidencia: pares ASS/voz con separación de exactamente 0,05 s (el `paso_min` de la guarda de monotonía desplegando palabras colapsadas); distribución del wpm implícito sobre 324 ventanas de dos producciones: p50 209-225, p99 268-300, patológicas 530 y 833 · arreglado repartiendo la ventana cuando supera 330 wpm · **descartado por medición**: reescalar la ventana cuando ocupa más de lo que se tarda en decirla empeora con todos los umbrales (ayer 3 → 6 → 14 → 23 ventanas malas con 1.5/1.25/1.15) · estado: pendiente (>=2)

- [COMA-02] 2026-08-11 · una-causa-tres-sintomas · El modelo entregó una historia de 5334 palabras con **10 comas** (0,2 por 100) y frases de mediana **42 palabras**; la del día anterior traía 448 comas (8,5 por 100) y frases de 13. `_ensure_breathing_commas` solo subió de 10 a 146 (2,7 por 100), **un tercio** de la densidad natural, porque solo inserta delante de conectores y con frases de 42 palabras no hay suficientes. De ahí salen encadenados los tres síntomas que parecían independientes: **101 pausas fuera de puntuación** (3,9/min frente a 1,1 el día anterior, mediana 0,44 s = la pausa que edge-tts se inventa), la voz a **202 wpm** (frente a 177 y 160,6) y el ratio vídeo/chunk **0,79** (7 min de gameplay sin usar) · consecuencia: `target_wpm` NO es calibrable, porque la velocidad la fija la densidad de comas y no una constante — con n=3 el rango medido es 160,6 / 177,2 / 202,3 · evidencia: `_clean_speech_for_tts` sobre las dos historias reales, conservadas en `data/evidence/` · estado: pendiente (>=2)
